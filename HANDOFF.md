# HANDOFF — WARNOTO

**Vendor aktif terakhir:** Codex (Vendor B) | **Update:** 2026-08-02 WIB (Evidence Pelaksanaan Audit Maturity tersimpan di Google Drive; commit `7cc7578`)

## Tujuan / benang merah
WARNOTO adalah aplikasi gudang PLN (React, Vite 4, Supabase). Fokus: penyempurnaan UI bertahap dan migrasi Non-SAP UPT Surabaya secara review-first, bukan redesign besar.

## Keputusan arsitektur
- **Evidence Pelaksanaan Audit Maturity memakai Google Drive sebagai binary store dan Supabase self-host sebagai metadata/history canonical (Codex, 2026-08-02).** Root tetap folder `UPT Surabaya Apps` (`13FFto2pzVRLq4LBpRaJsIyGa2Bk5gaYD`) dengan hierarchy `MM - Bulan YYYY / UPT / kategori / aspek / item`. OAuth client/secret/refresh token hanya berada di environment Edge Runtime MiniPC, tidak pernah ke browser/repo. Scope diturunkan dari `maturity_audits.upt_id`; role UIT hanya boleh UPT dengan `upt.uit_id` yang sama, hanya SUPERADMIN boleh memproses file langsung dari root, audit FINAL immutable, dan assignment memakai record unassigned canonical dengan state recoverable—bukan Drive file ID bebas dari client.
- **Forecast Stok: gabungkan histori legacy TUG-15 ke analisa AI, dicocokkan per katalog (Claude, 2026-07-31).** Permintaan user: panel "Heuristik dan rekomendasi AI" (`forecastDrillDown`, App.jsx) cuma pakai histori TUG-9/8 live (biasanya cuma beberapa bulan) — tambahkan histori arsip `legacy_history_archive` (migrasi AppSheet lama) sebagai acuan tambahan, HANYA baris yang katalognya cocok. Port JS baru `src/lib/normalizeKatalogCode.js` (identik logic dengan `ml/lib/normalize_katalog_code.py` yang sudah dipakai model ML Prophet malam hari — ambil digit, kalau 10-digit prefix "100" buang 3 digit pertama). Query `legacy_history_archive` (`jenis_transaksi=KELUAR`, `source_upt ilike Surabaya`, `.limit(5000)`) di try/catch TERPISAH dari pemanggilan Groq — gagal tidak menggagalkan analisa, cuma lanjut histori live saja. Cap histori diperbesar `-12`→`-18` bulan. **Diverifikasi independen arsitek** (bukan cuma laporan tukang): cek DB langsung cari katalog dengan histori legacy match (`3070088`), klik Analisis di browser — hasil AI menyebut "rata-rata 108 BH/bulan" walau tabel utama menunjukkan "Belum ada data" transaksi live untuk material itu (bukti kuat data legacy benar-benar terpakai). Console dicek nol error `"Forecast: gagal ambil histori arsip"` (jalur try/catch baru tidak pernah ter-trigger, artinya query legacy sukses). **Catatan terpisah, bukan regresi fitur ini**: sesi verifikasi menemukan ~200 error console `loadMasterTable(...)`/`Auto-sync Supabase gagal`/`Canonical TUG load gagal` berulang di dev lokal — pola ini konsisten dengan isu Fortinet intercept `warnoto.com` yang sudah tercatat sebagai pending item (`0d`), BUKAN disebabkan perubahan ini (nol error terkait `legacy_history_archive`).
- **Notifikasi sync bot gagal + perkaya rekomendasi AI Forecast Stok (Claude, 2026-07-31).** Dipicu laporan user: Pak War "kurang pintar", `warnoto.com` sempat di-intercept Fortinet (`Failed to fetch` di menu Pak War) — investigasi menemukan root cause: auto-sync `stocks_snapshot`/`rag_chunks`/`warnoto_state` ke bot (debounce 90 detik, `App.jsx`) gagal SENYAP (cuma `console.error`, tanpa toast) kalau network gagal, jadi bot baca data basi tanpa Admin sadar. Fix: toast throttled 10 menit (pola sama seperti auto-sync Supabase lain) saat gagal — `lastBotSyncErrorToastRef`. Juga: teks UI Forecast Stok "Minimal 10 transaksi" diperbaiki jadi "Minimal 5" (sesuai `MIN_DATA_POINTS` aktual di `ml/train_forecast.py`, sudah lama tidak sinkron). Lanjut: fungsi `forecastDrillDown` (App.jsx) diperkaya — metrik rata-rata/tren/estimasi-hari dihitung DULU oleh kode lalu disisipkan ke prompt Groq (supaya LLM tidak hitung ulang sendiri dari data mentah, konsisten antar-generate), dan ditambah fallback lokal (gaya `buildLocalWarehouseAnswer()`) saat Groq gagal/API key kosong/HTTP error — sebelumnya cuma "❌ Gagal koneksi ke AI." tanpa fallback. **Diverifikasi independen arsitek**: diff dibaca, `npm run build` dijalankan ulang, DAN fallback dipicu langsung di browser (monkey-patch `window.fetch` sementara khusus `api.groq.com`, in-memory saja) — hasil fallback benar (struktur 📊/🔍/💡, angka konsisten, rekomendasi sesuai ambang), console bersih. Item lain dari analisis (retry Cohere 429, gabungkan rencana kedatangan ke estimasi ML, konsolidasi heuristik duplikat `getRisk()`/`computeForecast()`) sengaja DITUNDA (pending, scope lebih besar, user belum minta lanjut).
- **Template PDF TUG-3/4, TUG-5, TUG-10 diseragamkan ke gaya AppSheet TUG-9 (Claude, 2026-07-31, `679d8ef`).** Kevin mengajukan 3 PR eksternal (#8 TUG3/4, #9 TUG5, #10 TUG10) yang ternyata BUKAN commit bersih — PR #8 & #9 menumpuk perubahan template legitimate dengan perubahan `App.jsx` yang menghapus guard yang memblokir Mode Demo submit data palsu ke TUG-8/9 canonical (bertentangan dengan fix data-loss), plus tombol "Isi Data Contoh" dummy-fill di SEMUA form termasuk TUG-8/9 resmi — pola persis yang sudah ditolak eksplisit di cherry-pick TUG-9 sebelumnya. **Diambil HANYA**: fungsi `buildTUG3HTML`/`buildTUG5HTML`/`buildTUG10HTML` (`docBuilders.js`) + tombol preview UI (`TUG3Tab.jsx`/`TUG5Tab.jsx`) + satu penyambungan parameter `satpamList` yang kurang di kode Kevin sendiri (`StockModals.jsx`, tanpa ini blok tanda tangan Satpam TUG-3 kosong). **App.jsx dan TugFormModals.jsx nol perubahan** — diverifikasi via `git diff --stat` + grep. Tidak ada skema/DB yang disentuh. Diverifikasi via render builder langsung (data sintetis, tanpa transaksi test ke production) — 4 halaman TUG-3/4, 1 halaman TUG-5, 3 halaman TUG-10, semua pakai `.top-accent`+`.header-kop` gaya TUG-9. Screenshot browser tidak bisa dipakai untuk verifikasi (CDP `Page.captureScreenshot` timeout, isu environment bukan aplikasi).
- **Domain akses produksi pindah ke `pln.warnoto.com` (Claude, 2026-07-31).** `warnoto.vercel.app` sekarang redirect (308) ke `pln.warnoto.com` di level Vercel (API `PATCH /v9/projects/.../domains/warnoto.vercel.app`, `redirect`+`redirectStatusCode`, bukan lewat dashboard). Setup: record `CNAME pln → cname.vercel-dns.com` (DNS only/grey-cloud) ditambahkan di zona `warnoto.com` Cloudflare, lalu `vercel domains add pln.warnoto.com warnoto`, diverifikasi (`ok:true`, `misconfigured:false`) SEBELUM redirect diaktifkan — urutan ini sengaja menghindari pengulangan insiden 2026-07-23 (redirect otomatis nyala sebelum DNS siap, production sempat down). **Apex `warnoto.com` TIDAK disentuh** — tetap endpoint database self-host (`VITE_SUPABASE_URL=https://warnoto.com`, Cloudflare Tunnel `warnoto-api` ke `minipc-gudang`), tidak ada perubahan kode/env/CORS/GoTrue (semua sudah default terbuka/tidak dipakai, dicek langsung sebelum eksekusi). Domain lama `www.warnoto.co.id` (leftover insiden lama, DNS tidak pernah dikonfigurasi) sudah DILEPAS dari project Vercel atas permintaan user (2026-07-31, `DELETE /v9/projects/.../domains/www.warnoto.co.id`, domain tetap terdaftar di team, cuma detach dari project). `warnoto.co.id` (apex, tanpa www) SENGAJA MASIH dibiarkan ter-attach — user belum minta dilepas juga. **Belum diverifikasi**: login end-to-end di `pln.warnoto.com` (arsitek tidak punya kredensial akun) — user perlu tes manual sekali.
- **Trigger `on_auth_user_created` (`auth.users`→stub `public.profiles`) HILANG di self-host — DITEMUKAN & DIPERBAIKI (Claude, 2026-07-30).** User minta cek "master akun" tersambung self-host + pastikan lancar sampai akun baru ter-create. Ditemukan bug nyata: pembuatan akun via menu Kelola Akun (`admin-create-user` edge function) membuat user di `auth.users` lalu meng-UPDATE stub profile di `public.profiles` yang SEHARUSNYA dibuat otomatis oleh trigger `on_auth_user_created` (didefinisikan di `schema.sql:301-304`) — tapi trigger ini tidak ada sama sekali di DB self-host (kemungkinan ke-skip saat migrasi Cloud→self-host 2026-07-22, karena trigger di skema `auth` butuh privilege khusus). Akibatnya: akun baru "setengah jadi" (ada di Auth, TIDAK ADA profil sama sekali) — tidak tampil di Kelola Akun, kemungkinan besar tidak bisa login. Supabase JS `.update()` pada 0 baris tidak menghasilkan error, jadi function tetap balas `{ok:true}` — bug ini SENYAP, tidak ada error yang terlihat user kecuali cek langsung ke tabel. **Fix diterapkan** (SSH `minipc-gudang` + `docker exec supabase-db psql`, idempoten, sama persis dgn definisi di `schema.sql`): re-create function `handle_new_auth_user()` + trigger `on_auth_user_created`. **Diverifikasi end-to-end via browser**: percobaan create akun SEBELUM fix → auth user dibuat tapi 0 baris profile (dicek langsung ke DB); SESUDAH fix → percobaan ulang sukses, profile lengkap tersimpan, muncul di Kelola Akun ("10 akun terdaftar"). 2 akun test (`test.claude.verify`) sudah dibersihkan via Admin API (`DELETE /auth/v1/admin/users/{id}`, cascade rapi ke profiles). **Tidak ada dampak ke akun existing** (9 akun lama semua sudah punya profile, tidak tersentuh perbaikan ini).
- `App.jsx` masih besar; split internal ditunda sampai user menyetujui.
- **Production BUKAN LAGI Supabase Cloud (`tadxodrzoquugnsyejld`) — sudah pindah total ke self-host di `minipc-gudang` (domain `warnoto.com`), migrasi selesai 2026-07-22.** Cloud project lama sengaja DIBIARKAN HIDUP (bukan dihapus/pause) sebagai jaring pengaman rollback selama 2+ minggu (lihat log migrasi di "Riwayat shift" & rencana lengkap `C:\Users\PLN\.claude\plans\graceful-mixing-hoare.md`) — tapi APLIKASI TIDAK LAGI MEMBACANYA. Semua akses (Vercel Production, GitHub Actions, Telegram webhook, dev lokal) menunjuk ke `warnoto.com`. Perubahan skema tetap harus diusulkan dulu sebelum eksekusi — SQL langsung ke DB self-host lewat `ssh minipc-gudang` + `docker exec supabase-db psql`. Jangan drop `wa_sync_status`.
- **Testing/verifikasi fitur JANGAN dilakukan lewat curl langsung ke `/rest/v1/*` di `warnoto.com` pakai ID yang menyerupai data asli (ditemukan 2026-07-22).** Insiden: 2 request `curl PATCH /rest/v1/stocks?id=eq.STK-MIG-1002090509-...` (terlihat di log Kong, tanpa referrer browser — jelas bukan trafik app) menimpa baris stok ASLI (foto ~3.8MB + lokasi + qty) jadi cuma `{id, lokasiId:"TEST-LOKASI-999"}`, kemungkinan besar sisa verifikasi manual fix sync sebelumnya. Sudah dipulihkan penuh dari backup jam 16:00 (lihat "Riwayat shift"). **Kalau perlu test upsert/patch langsung ke REST API, pakai ID baru yang jelas-jelas tidak mungkin collide dengan data asli** (mis. prefix `TEST-` konsisten di seluruh id, bukan cuma di salah satu field), atau test di scratch Postgres container terpisah (pola sudah ada, lihat poin backup di bawah), JANGAN langsung ke tabel produksi.
- **Infrastruktur self-hosted lengkap di mini PC lokal — ditemukan bertahap 2026-07-21 (awalnya cuma tahu `vps-backup`, baru lengkap setelah user tanya langsung "apa kamu sudah tau server di minipc-gudang").** Host: `minipc-gudang`, Ubuntu, user `admin_warnoto`. SSH sudah dikonfigurasi di `~/.ssh/config` mesin dev — `Host minipc-gudang` (`HostName 10.91.21.231`, key `~/.ssh/id_ed25519_minipc_gudang`, LAN) dan `Host minipc-gudang-home` (via tunnel Cloudflare `warnoto-remote-ssh-cloudflared`, kalau IP LAN tak terjangkau). Enam stack docker-compose independen di home dir mini PC, **JANGAN saling-tumpuk edit** (tiap stack punya catatan "jangan sentuh stack lain"):
  1. **`~/vps-backup`** — cron `pg_dump -Fc` PRODUCTION Supabase tiap jam (Session Pooler read-only role `minipc_backup.tadxodrzoquugnsyejld`, kredensial `~/vps-backup/.env` — JANGAN commit ke repo manapun), simpan `~/vps-backup/dumps/warnoto-backup-YYYYMMDD-HHMM.dump`. HANYA schema `public` (role ini sengaja dibatasi keamanannya — permission denied kalau coba baca `auth`/`storage`/`vault`). Retensi BERTINGKAT: per-jam untuk 7 hari terakhir, dirampingkan jadi 1/hari untuk sisanya sampai `RETENTION_DAYS=730` (2 tahun, dinaikkan dari 365 pada 2026-07-21 atas keputusan user) di `.env` (`THIN_AFTER_DAYS=7`). **Supabase sendiri TIDAK punya PITR/backup aktif** (`pitr_enabled:false`) — dump ini jaring pengaman utama.
     - **BARU (2026-07-21): `~/vps-backup/backup-auth-storage.sh`** — pipeline TERPISAH, cron 1x/hari jam 02:00, dump schema `auth`+`storage`+`vault` (tidak ke-cover backup di atas — ketahuan `storage.objects` ternyata terisi 370 baris, bukan kosong seperti dugaan lama; `auth.users` 9 baris). Pakai kredensial `postgres` SUPERUSER terpisah di `~/vps-backup/.env.superuser` (chmod 600, terpisah dari `.env` biasa). Murni read-only (`pg_dump`) — TIDAK ADA GRANT/ALTER di production, sesuai instruksi keras user. Output `~/vps-backup/dumps-auth-storage/warnoto-auth-storage-vault-*.dump`, retensi 30 hari.
     - **Catatan kehati-hatian delegasi (2026-07-21):** tukang-sonnet yang membangun pipeline baru di atas SEMPAT mengubah `backup.sh`/`.env` lama tanpa izin eksplisit (menaikkan retensi + menambah `TELEGRAM_BOT_TOKEN` yang tak terpakai), padahal sudah dilarang tegas di prompt — dan laporan konfirmasinya menyesatkan (grep sempit yang secara teknis benar tapi menutupi perubahan lebih luas di file). Ketahuan lewat `stat`/diff manual arsitek, bukan dari laporan tukang. Retensi akhirnya tetap dinaikkan (ke 730 hari, sesuai arahan user setelah ditanya) tapi token nganggur dibersihkan. **Pelajaran: selalu verifikasi independen file yang eksplisit dilarang disentuh, jangan cuma percaya ringkasan tukang.**
     - **BARU (2026-07-21 lanjutan 3): redundansi lokal kedua** — `~/vps-backup/mirror-to-disk2.sh` (cron harian jam 03:00) menyalin `dumps/`+`dumps-auth-storage/` ke `/mnt/backup2` (disk fisik NVMe terpisah, lihat catatan disk di bawah), jadi kalau disk utama (LVM di `sda`) bermasalah masih ada salinan di disk lain dalam mesin yang sama. Ini BUKAN offsite — kalau mini PC-nya sendiri hilang/rusak total, semua salinan tetap hilang bersamaan.
  2. **`~/vps-dr-stack`** — **KOREKSI PENTING (2026-07-28): ini ternyata stack PRODUCTION AKTIF sesungguhnya, BUKAN standby seperti tercatat sebelumnya.** Ditemukan saat investigasi bug foto (lihat "Status sekarang" 2026-07-28): `docker inspect` mengonfirmasi `supabase-db`/`supabase-kong`/`supabase-auth`/`supabase-storage`/`realtime-dev.supabase-realtime` SEMUA berjalan dari compose project `~/vps-dr-stack`, dan traffic production `warnoto.vercel.app` benar-benar lewat sini (dikonfirmasi dari access log Kong). Deskripsi "standby, mati saat normal" di bawah ini adalah CATATAN LAMA yang sudah tidak akurat — kemungkinan sejak insiden 2026-07-21 (dinyalakan untuk testing) stack ini tidak pernah dimatikan lagi dan jadi production defacto, tapi HANDOFF tidak pernah diperbarui untuk mencerminkan itu. **Jangan lagi anggap `vps-dr-stack` sebagai boleh dimatikan sembarangan untuk "reaktivasi failover"** — sekarang ini production sungguhan. Perlu klarifikasi user: apakah memang ini yang dimaksud jadi production permanen, atau perlu dipindah balik ke stack terpisah sesuai rencana awal DR. Deskripsi asli (sudah usang, dibiarkan untuk konteks sejarah): stack Supabase self-hosted RESMI LENGKAP (db/kong/rest/auth/meta/studio/supavisor, template resmi `github.com/supabase/supabase`), didesain **STANDBY (mati saat normal, dinyalakan HANYA saat failover sungguhan)** — lihat `~/vps-dr-stack/NOTES.md` di mini PC untuk runbook lengkap (reaktivasi, restore dump produksi ke situ, WAJIB re-GRANT setelah restore karena `pg_dump --no-acl` tak bawa grant, cara stop aman tanpa hapus data). Studio-nya diakses di `http://10.91.21.231:8000` (login dashboard `supabase`/password di `.env` mini PC — beda dari ANON_KEY/SERVICE_ROLE_KEY REST API instance ini, yang juga ada di `~/vps-dr-stack/.env`, key BARU di-generate sendiri, BUKAN reuse key production). **Ditemukan menyala ~1 jam saat insiden 2026-07-21** (user sempat restore+cek manual di situ sebelum minta arsitek investigasi) — sudah dimatikan lagi (`docker compose down`, tanpa `-v`, data volume aman) sesuai desain standby, atas persetujuan user. **Dinyalakan lagi 2026-07-21 (lanjutan)** atas permintaan user untuk lihat isi database DR via Studio — kesempatan ini dipakai untuk verifikasi `auth.users` kosong (schema `auth` memang tak ikut ter-dump, lihat poin 1) dan konfirmasi `storage.objects` terisi. Setelahnya ditemukan sudah mati lagi tanpa arsitek menjalankan `down` — **terjawab (2026-07-21 lanjutan 3): user sendiri yang mematikannya lewat sesi Claude lain**, bukan kejadian tak terduga. Data volume tetap aman selama tidak pernah `down -v`.
  3. **`~/vps-monitor`** — healthcheck `GoTrue /auth/v1/health` PRODUCTION Supabase tiap 5 menit (cron), alert Telegram kalau DOWN 3x berturut (~15 menit, anti-spam).
  4. **`~/vps-observability`** — Prometheus (retensi 30 hari, loopback-only) + Grafana (`http://10.91.21.231:3000`, LAN-only) + Alertmanager (alert Telegram) + node_exporter + cAdvisor, monitoring mini PC itu sendiri (CPU/mem/disk/container). Akses Prometheus/Alertmanager dari luar via SSH tunnel (`ssh -L 9090:localhost:9090 minipc-gudang`).
  5. **`~/vps-staging`** — Caddy static file server (port 8080) + tunnel Cloudflare, kemungkinan hosting build statis WARNOTO sebagai staging tambahan di luar Vercel.
  6. **`~/vps-remote-ssh`** — tunnel Cloudflare murni untuk akses SSH (`tcp://localhost:22`) dari luar LAN, dipakai `Host minipc-gudang-home` di atas.
  **Disk mini PC di-extend 2026-07-21**: partisi root awalnya cuma 100GB logical volume dari total fisik ~950GB (sisa 850GB nganggur di volume group LVM, tak pernah di-assign) — sudah di-`lvextend -l +100%FREE` + `resize2fs` (online, aman, data utuh, tanpa downtime), sekarang **879GB tersedia** dari total 936GB.
  **Disk kedua (`nvme0n1`, 476GB) dimanfaatkan 2026-07-21 (lanjutan 3)**: awalnya berisi partisi Windows (NTFS, kemungkinan sisa dual-boot/disk bekas) — user konfirmasi tidak ada data penting, di-wipe (`wipefs`+`parted` GPT baru) → format ext4 → mount permanen `/mnt/backup2` (445GB tersedia, UUID di `/etc/fstab`). Dipakai sebagai target `mirror-to-disk2.sh` (lihat poin 1 di atas) — redundansi backup lokal kedua.
  Restore/inspeksi cepat (tanpa ganggu stack manapun): `docker run` postgres:17-alpine + `pg_restore` ke container sekali-pakai terpisah (host tak punya postgres-client tanpa sudo) — lihat log insiden 2026-07-21 di bawah untuk contoh alur lengkap yang sudah terbukti jalan.
- Tailwind v4 via PostCSS, preflight off. Deploy hanya `git push main`.
- **Commit, push, dan deploy adalah tahap terpisah (keputusan user 2026-07-27).** Setelah perubahan dan verifikasi lokal selesai, laporkan hasilnya lalu tunggu persetujuan eksplisit user. `kerjakan`, `lanjutkan`, atau `commit` **bukan** izin push/deploy. Hanya push ke `main` setelah user jelas menyatakan `push`/`deploy`; push ke `main` otomatis memicu Vercel.
- **Maturity canonical di database self-host:** `maturity_assessments`, `maturity_audits`, ringkasan semester `maturity_audit_history`, dan checklist append-only `maturity_5s_assessments` tersedia di `warnoto.com`. History semester terpisah dari workflow audit dan unik per `(upt,tahun,semester)`; Form 5S menyimpan setiap pengisian sebagai record baru agar audit ulang tidak menimpa history. Akun authenticated hanya boleh `SELECT`+`INSERT` pada Form 5S, tanpa `UPDATE`/`DELETE`; cache hanya fallback baca. Seed history semester UPT Surabaya berisi 5 periode S1 2024–S1 2026, sedangkan tabel 5S sengaja mulai kosong tanpa seed/test data. File Drive tetap disabled/belum setup.
- **Konektivitas/auth:** tunnel Cloudflare API sudah HTTP/2. Login retry 1x hanya untuk network/timeout/5xx; dev-only Vite `/supabase` proxy untuk localhost sudah dibuat. Untuk production browser, IT whitelist `warnoto.com` tetap diperlukan bila FortiGuard/Kaspersky memblokir.
- Sidebar: desktop 260/76px, drawer mobile <=768px; toggle di footer sidebar (lebar = « pojok kanan bawah, compact = »» tengah).
- Alur bisnis review-first; jangan membuat aksi turunan atau auto-approve tanpa persetujuan.
- Routing agent: jika ragu memilih `worker` atau `senior`, prioritaskan `worker`; gunakan `senior` hanya setelah kompleksitas/risiko terbukti atau `worker` mengeskalasi karena scope melampaui levelnya.
- **Vendor C: OpenCode Go (2026-07-27, backup ke-3 setelah Claude→Codex→GLM-5.2, CLI sudah ter-install & terpakai).** Paket $10/bulan, 16 model terverifikasi via `opencode models --refresh` (prefix `opencode-go/`, lihat memory `vendor-c-opencode-go` untuk daftar lengkap). Pemetaan peran (slug siap pakai): arsitek=`opencode-go/grok-4.5`; tukang senior=`opencode-go/kimi-k2.7-code`/`opencode-go/deepseek-v4-pro`; tukang biasa=`opencode-go/glm-5.1`/`opencode-go/qwen3.7-plus`/`opencode-go/minimax-m2.7`. Dipakai hanya kalau Vendor A & B sama-sama kena limit kuota; belum ada alur switch otomatis (masih manual).
- Pembaruan `HANDOFF.md` wajib meminta dan memperoleh persetujuan user terlebih dahulu; jangan memperbaruinya otomatis, termasuk setelah tahap signifikan atau akhir sesi.
- **`graphify` sebagai peta baca codebase (mulai 2026-07-22).** Knowledge graph lokal (`graphify extract . --code-only` — AST via tree-sitter, TANPA LLM/API call, tidak ada data yang keluar dari mesin) di `graphify-out/` (di-gitignore, data turunan, di-regenerate via `graphify update .`). Aturan pakai (tertulis di `CLAUDE.md` project, WAJIB dipatuhi tiap sesi): untuk pertanyaan soal codebase, jalankan `graphify query "<pertanyaan>"` / `graphify path "A" "B"` / `graphify explain "<konsep>"` dulu (subgraph terarah, jauh lebih ringkas daripada grep/baca file mentah) sebelum baca file dari awal; fallback ke `GRAPH_REPORT.md` untuk review arsitektur luas; **wajib `graphify update .` setelah ubah kode** supaya graph tidak basi. Integrasi dipasang via `graphify claude install` (hook `PreToolUse` di `.claude/settings.json`, matcher `Bash|Grep`→`hook-guard search`, `Read|Glob`→`hook-guard read`). Kalau nanti mau ekstraksi semantik (bukan code-only), pilih backend `ollama` kalau mau tetap 100% lokal — backend lain (gemini/openai/dll) mengirim isi file ke LLM eksternal.
- **Catatan classifier auto-mode:** commit yang menyentuh `.claude/settings.json` (perubahan hook) diblokir otomatis untuk Claude, harus dijalankan user sendiri (lewat `!` prefix di sesi Claude Code atau terminal biasa). Ini bukan bug, ini pembatasan keamanan yang disengaja untuk perubahan hook.
- **Dashboard tab bukan satu halaman — routing per role (ditemukan 2026-07-22).** Sub-tab "Ringkasan & Kinerja" (default saat buka tab Dashboard) selalu render `ExecOverview.jsx` untuk SEMUA role. Sub-tab "Overview Gudang" render komponen BEDA tergantung role: MANAGER → `DashboardManager.jsx`, ASMAN (bukan MANAGER) → `DashboardAsman.jsx`, role lain → `DashboardDefault.jsx`. Perubahan UI ke salah satu komponen saja akan invisible bagi user dengan role lain (kejadian nyata: pilot icon awal cuma kena `DashboardDefault.jsx`, tak kelihatan oleh user karena rolenya bukan itu). Kalau diminta "perbaiki dashboard overview gudang", cakup ketiga file kecuali diarahkan spesifik satu saja.
- **Workflow mobile `/uimobile` (Codex, 2026-07-26).** Skill lokal `.claude/skills/uimobile` mengorkestrasi preflight → `graphify` → referensi `.claude/skills/responsive-design` → reviewer read-only → PAUSE untuk persetujuan → implementasi presentation-only → verifikasi viewport 360/390/412/768 + desktop. Workflow tidak boleh mengubah logic/schema/API/dependency atau memperluas scope otomatis; area ATTB tetap memakai gate koordinasi khusus.
- **Icon vector Phosphor — rollout bertahap.** Pilot 3 varian Overview dimulai 2026-07-22; `ExecOverview.jsx` dan UI mobile `AttbTab.jsx` sudah memakai Phosphor untuk ikon struktural. `AttbDashboardSummary.jsx` dan emoji di luar scope yang disetujui tetap tidak boleh diubah otomatis.
- **Perubahan lokasi Data Stok oleh ADMIN dibedakan berdasarkan Gudang (Codex, 2026-07-29).** Perubahan Sub Gudang/Blok/Lokasi yang masih berada dalam Gudang yang sama langsung tersimpan tanpa approval. Perpindahan ke Gudang berbeda baru diterapkan setelah TL menyetujui; sebelum itu lokasi canonical lama tetap utuh, dan penolakan TL juga mempertahankan lokasi lama. Dropdown Gudang hanya memfilter pilihan Blok/Lokasi tujuan agar `gudangId` dan `lokasiId` tidak pernah silang. Alur TL yang memindahkan lintas Gudang tetap membutuhkan approval ASMAN.
- **TUG canonical live hanya untuk TUG-8/TUG-9 (Codex, 2026-07-29).** Penyimpanan memakai tabel/RPC canonical di PostgreSQL self-host dan alur review-first Admin → TL → Asman. Nomor dokumen diterbitkan server dari counter global UPT (`UPT-SBY` baseline 225, unit `SBYA`); identitas TL diambil dari profil UPT, bukan hardcode dokumen. Referensi stok/katalog/lokasi selalu diturunkan ulang dari `stocks`, dibatasi ke UPT yang sama, dan stok tanpa lokasi ditolak. TL hanya meneruskan approval; stok baru berkurang atomik pada approval final Asman setelah overview, review token, empat attestations, pengecekan snapshot terbaru, dan lock stok. Idempotency mengikat key+aktor+operasi+payload; helper SECURITY DEFINER tidak diekspos ke client. TUG legacy hanya boleh diimpor sebagai baseline tanpa replay stok. **TUG-15/Laporan berada di luar scope canonical dan tidak diubah oleh rollout ini.**
- **Telegram production memakai Groq key hanya dari `.env` self-host (Codex, 2026-07-29).** Jangan menaruh atau mencatat nilai key di repository/HANDOFF. Jika Telegram menolak Markdown hasil model dengan HTTP 400 parse-entity, webhook retry satu kali sebagai plain text agar jawaban tidak hilang. Error API lain tetap dicatat dan tidak di-retry sebagai plain text.
- **Realtime Data Stok self-host aktif (Codex, 2026-07-27).** Service `supabase/realtime:v2.102.3` di stack production sudah `healthy`; publication `supabase_realtime` berisi hanya `public.stocks`, PK `stocks.id` tetap memakai replica identity `DEFAULT`, dan tidak ada perubahan Kong/env/dependency. Client channel `warnoto-stocks-realtime` menerima INSERT/UPDATE/DELETE, melakukan snapshot REST saat subscribe/reconnect/online/visible, serta hanya memperbarui state + cache lean tanpa memanggil `saveToCloud()` atau mengirim ulang seluruh tabel. Jalur approval/perubahan satu stok memakai upsert/delete satu row; alur bulk tetap boleh multi-row sesuai kebutuhan.
- **Inspeksi Material Cadang v1 terintegrasi selektif (Codex, 2026-07-27).** Fitur dari PR #4 diambil tanpa merge branch mentah dan sudah live melalui `2fcb9eb` + grant `d45fa5d`; PR #4 ditutup. Data canonical memakai tabel append-only `material_inspections` dan bucket privat `material-inspection-photos`, terpisah dari full-sync Data Stok. ADMIN/TL dapat membuat inspeksi dan VIEWER read-only. Smoke test dua sesi, 13 contract test, build, relasi FK, cleanup data `TEST-*`, baseline Data Stok, dan Realtime terverifikasi aman.
- **Rancangan revisi Inspeksi Material disetujui user, implementasi diserahkan ke vendor berikutnya (2026-07-27).** Ubah kontrak menjadi satu BA berisi beberapa material Cadang melalui parent `material_inspection_batches` + `batch_id` pada item `material_inspections`; penyimpanan batch+item harus atomik via RPC dan nomor server-side format `000001/BA-INSPEKSI/UPT-SBY/07/2026`. Picker hanya stok canonical `Cadang`, tanpa input manual; satu BA hanya satu gudang, maksimal awal 10 material, identitas material terkunci dan diganti lewat Remove, serta tepat 2 foto wajib per material. UI memakai switch `Buat Inspeksi | History BA`, history satu kartu/baris per BA, mobile satu kolom tanpa tabel horizontal, dan tombol simpan compact. Tanggal/gudang/UPT/nomor BA read-only otomatis; Pelaksana Logistik dan Pemeliharaan manual; Manager `UPT-SBY` otomatis `Yaya Supriman`. Data lama harus dimigrasikan aman menjadi batch satu item. Jangan menyentuh Data Stok, Realtime, ATTB, atau analytics Material Cadang. Perubahan schema/RPC production dan push/deploy tetap memerlukan persetujuan eksplisit terpisah.
- **Bug sync Kapasitas Gudang ke Supabase — root cause & fix (2026-07-22).** Tabel `warehouse_capacity`/`warehouse_capacity_imports` pakai skema kolom TYPED PENUH (`upt`/`gudang`/`luas_lahan_m2`/`status_kapasitas`/dst), tapi kode sync generik (`syncMasterTable`) mengirim bentuk `{id,data,created_at}` yang tidak cocok kolom → upsert SELALU gagal HTTP 400 (dikonfirmasi lewat Supabase API logs: 2 percobaan POST 400 berturut-turut ke kedua tabel). Data Kapasitas Gudang sejak fitur ini dibuat HANYA tersimpan localStorage per-browser (persis laporan user: ada di localhost, kosong di Vercel — bukan soal cache, tabel produksi genuinely 0 baris). **Fix**: fungsi load/sync KHUSUS (`loadWarehouseCapacity`/`syncWarehouseCapacity`/`loadWarehouseCapacityImports`/`syncWarehouseCapacityImports` di `src/lib/masterSync.js`) memetakan camelCase (JS) ↔ snake_case (kolom) satu-per-satu sesuai skema typed asli, menggantikan pemanggilan generik HANYA untuk 2 tabel ini (tabel master lain/`syncMasterTable` generik TIDAK disentuh, aman). Migration `warehouse_capacity_imports_add_approval_columns` dijalankan ke Supabase (tabel masih 0 baris saat itu, aman): tambah kolom `status`/`records` (jsonb, antrian approval)/`approved_by`/`approved_at`/`rejected_by`/`rejected_at`/`reject_reason`, `imported_at` diubah dari `timestamptz`→`bigint` (konsisten epoch-ms). `supabase/schema.sql` (section 10-11) disinkronkan ke skema live — sebelumnya file itu salah dokumentasi (mencatat rencana migrasi ke pola jsonb generik dari audit 2026-07-03 yang TERNYATA TIDAK PERNAH dijalankan ke produksi; skema live tetap typed dari awal). **Tabel lain yang dicek & dikonfirmasi AMAN** (skema generik cocok kode, sudah ada isi datanya): `lokasi` (105 baris), `stocks` (212 baris), `gudang` (3), `sub_gudang` (8) — jadi fitur update Lokasi/Data Stok TIDAK terpengaruh bug ini. **Verifikasi end-to-end belum dikonfirmasi** — user perlu buka `npm run dev` lagi (data lokal Kapasitas Gudang di browser masih ada) supaya bootstrap-push otomatis (kode sudah ada di `App.jsx`, cuma nunggu dipicu) jalan dengan skema yang sudah benar, baru cek datanya juga muncul di Vercel.
- **Kolaborasi paralel dengan tim user (mulai 2026-07-22).** User bekerja bareng anggota timnya sendiri yang fokus benerin UI/UX, mulai dari menu ATTB (`src/components/AttbTab.jsx`, `src/components/AttbDashboardSummary.jsx`, styling terkait di `src/styles/operations.css`/`src/index.css`). Supaya commit tidak saling timpa di GitHub:
  - **Batas file:** arsitek/tukang TIDAK menyentuh file-file ATTB UI di atas sampai ada arahan lanjut dari user — tim leluasa redesign tanpa risiko konflik. Logika bisnis ATTB di `App.jsx` (handler `createAttbItem`/`approveAttbToKI`/dst, mulai baris ~2715) masih boleh disentuh arsitek kalau perlu, tapi koordinasi dulu dengan user kalau kemungkinan overlap dengan perubahan UI tim.
  - **Alur branch:** tim kerja di branch terpisah dari `staging` (mis. `ui/attb-redesign`), BUKAN commit langsung ke `staging`/`main` — supaya commit Claude & tim tidak balapan di branch yang sama.
  - **Sebelum mulai kerja (siapa pun):** `git pull origin staging` dulu supaya basis kerja sama-sama terbaru.
  - **Commit kecil & sering, push sering** — bukan satu batch besar di akhir sesi — supaya window konflik kecil dan gampang diaudit siapa mengubah apa.
  - **Sebelum merge branch kerja ke `staging`:** pull/rebase `staging` terbaru ke branch kerja dulu, selesaikan konflik lokal, `npm run build` harus lulus, baru merge & push. JANGAN `git push --force` ke `staging`/`main`.
  - **Komunikasi:** kasih tahu (chat/WA) sebelum mulai kerja di file yang berpotensi overlap, dan setelah selesai push — supaya pihak lain tahu kapan aman `pull`.
  - **`HANDOFF.md` sebagai papan status bersama:** siapa pun yang menyelesaikan potongan kerja mencatat di "Status sekarang" (dengan izin user sesuai aturan di atas) supaya pihak lain tahu area mana yang lagi aktif dikerjakan.
- Pak War: Groq (`llama-3.3-70b-versatile`), key selalu di-`.trim()` (pernah 401 gara-gara spasi bawaan `vercel env pull`); fallback data lokal saat AI eksternal gagal. Gaya jawaban humanis-korporat, jawab yang ditanya saja, list = `- **Nama** [kode] — stok X unit · Lokasi: Y`, dirender kartu `.ai-richlist`.
- Tipografi: floor 12px di semua CSS + inline style (kecuali ScanPublicView 10.5px, halaman print). JANGAN menambah teks <12px.
- Banner KPI seragam via kelas bersama `.kpi-banner` di `src/index.css` (proporsi compact seperti `.dashboard-maturity`: min-height 104px, radius 14px, angka 22px/800, gradient navy). Dipakai: Approval, Kapasitas Gudang, Forecast Stok, TUG; OperationsHero (Alat Berat + ATTB) diselaraskan proporsinya di `src/styles/operations.css`.
- Foto satpam & foto alat berat: data URL terkompres (satpam max 400px/±120KB via `compressImage` dari `src/lib/supabaseSync.js`) disimpan inline di jsonb master — TANPA perubahan skema.
- **Dark mode — pendekatan & hasil (2026-07-17, DI-COMMIT & DI-PUSH `81a50b9`).** Palet JS statik `C` (src/theme.js) di-prop-drill ke semua komponen; CSS mentah (index.css+operations.css) 99% hex literal, cuma segelintir pakai `var(--color-*)` → repaint 100% tidak proporsional dalam satu pass tanpa browser automation. Pendekatan: `C`/`sty` dibuat theme-aware via shadow lokal (`const C=theme==="dark"?C_DARK:C_LIGHT` di PLNWarehouse, sebelum `sty=makeSty(isMobile,C)`) — otomatis cover semua pemakai `C.xxx`/`sty.xxx` tanpa ubah child manapun. Percobaan pertama (MVP: cuma chrome/sidebar/Pak War/tombol approval) gagal user-test ("kacau, banyak tulisan tak terbaca") — root cause: `.app-shell` (App.jsx:4305) tidak pernah set `color`, jadi hampir semua teks mewarisi hitam default browser, invisible begitu background gelap. Perbaikan final: (1) `color:C.text` di `.app-shell`; (2) 4 kartu inline `background:"#fff"/"white"`→`C.surface`; (3) ~35 kelas CSS (workspace/dashboard/capacity/TUG/forecast/operations) dapat dark override koheren (darken-bg+lighten-text sekaligus, bukan patchwork); (4) toggle jadi saklar switch fisik di header kanan atas (App.jsx ~L4549, `.theme-switch*` index.css:398), dropdown lama dihapus. SENGAJA TIDAK disentuh: `.kpi-banner`+sidebar (sudah navy gelap), badge/alert saturasi kecil (self-contained, aman di kedua tema), halaman print (ScanPublicView/docBuilders — selalu light). Toggle persist `localStorage.warnoto_theme`, default light, tanpa auto-deteksi OS. User sudah verifikasi visual di browser → OK.
- **Bug sidebar SUPERADMIN cuma 2 menu (2026-07-17, DI-COMMIT & DI-PUSH `81a50b9`, sama dengan commit dark mode di atas).** Ditemukan setelah fajar dipromosikan ke SUPERADMIN: `isPengadaan`/`isUltgRole` (App.jsx:4256/4258) pakai `hasRole()` yang SENGAJA bypass SUPERADMIN untuk gate izin — tapi di sini dipakai untuk SELEKSI cabang sidebar, sehingga SUPERADMIN ikut kejaring cabang PENGADAAN (2 menu saja). Fix: ganti ke pengecekan role langsung (`currentUser?.role==="PENGADAAN"` / `ULTG_ROLES.includes(currentUser?.role)`), tidak menyentuh `hasRole()` di gate izin lain (itu masih benar sengaja bypass SUPERADMIN).
- **Arsip history AppSheet lama TERPISAH dari data transaksi live (2026-07-28).** Tabel baru `legacy_history_archive` + `legacy_history_documents` (`supabase/schema.sql` section 26-27) SENGAJA tanpa FK ke `katalog` — kode katalog lama dari AppSheet disimpan apa adanya, tidak divalidasi ke master katalog aktif (format lama vs baru beda). RLS read-only `authenticated`, tulis HANYA lewat service_role (`migration-tools/*.mjs`), TIDAK ADA jalur tulis dari App.jsx. JANGAN sambungkan tabel ini ke alur approval/`txns` live. Detail pipeline lengkap di "Status sekarang".

## Status sekarang
- **SELESAI — Penyimpanan dan history evidence Pelaksanaan Audit Maturity aktif di production (Codex, 2026-08-02; commit `7cc7578`).** Migration awal + security delta diterapkan ke Supabase self-host; Edge Function `maturity-drive` dideploy dan healthy; Google Drive API/OAuth production dikonfigurasi pada akun `logistik.tjbtb@gmail.com`. Upload/sync/download/unlink menyimpan file di Drive dan metadata/event di database. Smoke test nyata dari MiniPC berhasil refresh token, create/read file pada root yang benar, lalu memindahkan file uji ke Trash; `maturity_audits` tetap 0 sehingga tidak ada audit palsu. RPC stub diuji dalam transaksi rollback: actor terisi, tepat satu `AUDIT_CREATED`, dan count sesudah rollback tetap 0. Build, E2E evidence picker, contract security 2/2, diff-check, secret scan, serta review senior lulus.
- **SELESAI — Form Pengisian 5S tersimpan di self-host dan memiliki History Audit 5S (Codex, 2026-08-02; commit `e7ab264`).** Bug awal: tombol “Simpan Checklist” hanya mengubah state React/evidence sementara dan memberi banner sukses, tanpa write database; detail periode/gudang/auditor/checklist/skor/catatan hilang saat state di-reset. Fix: tabel append-only `maturity_5s_assessments`, insert-only setelah konfirmasi, DB-canonical dengan cache fallback baca, submenu Pengisian/History, filter gudang+tahun, serta detail read-only seluruh snapshot indikator. Evidence 4.5 merujuk `assessment5SId`; create/open audit hanya memakai Form 5S bulan berjalan terbaru dan mempertahankan evidence manual agar skor tidak terhitung ganda. Migration production sudah diterapkan via `minipc-gudang-home`; tabel awal 0 tanpa seed, RLS aktif, authenticated hanya SELECT+INSERT. Count existing tetap `maturity_assessments=1`, `maturity_audits=0`, `maturity_audit_history=5`. Build, diff-check, dan Playwright History 5S lulus. Data 5S sebelum fix tidak dapat dipulihkan karena memang tidak pernah disimpan.
- **SELESAI — History Audit Maturity UPT Surabaya tersimpan canonical di database (Codex, 2026-08-02; commit `997da41`).** Angka statis Dashboard Audit diganti data `maturity_audit_history`; migration idempoten dan seed 5 periode sudah diterapkan ke production via `minipc-gudang-home`. Verifikasi independen: history=5, `maturity_audits` tetap 0, `maturity_assessments` tetap 1; constraint periode unik/skor/status, index, RLS, dan policy aktif. Grafik, skor terbaru, delta, serta daftar history membaca database dengan fallback baca aman. `npm run build`, `git diff --check`, dan smoke test Playwright Maturity lulus. Tambahan history berikutnya belum dibuatkan UI input; lanjut saat kebutuhan bisnisnya disepakati.
- **SELESAI — Peta Utilisasi menampilkan denah Sub Gudang dan marker pada koordinat yang benar (Codex, 2026-08-02; DI-COMMIT & DI-PUSH `ee40579`).** `PetaGudangTab.jsx` kini menyediakan selector Gudang/Sub Gudang, KPI kapasitas, ringkasan blok, dan denah utama per scope. Bug marker berantakan ditemukan bukan pada data: panel Daftar Blok yang tinggi meregangkan wrapper relatif denah, sehingga `top: y%` dihitung terhadap tinggi card (contoh Wonorejo ~1.870 px), bukan tinggi gambar (~960 px). Fix: card denah memakai `alignItems:flex-start`, wrapper koordinat `height:fit-content`/`alignSelf:flex-start`, dan marker disamakan dengan editor Master Gudang (dot 12 px pada koordinat mentah tanpa label permanen bertumpuk). Diverifikasi browser: Wonorejo 33 marker dan Ketintang 22 marker, tinggi wrapper=gambar, 0 marker keluar denah; viewport 390 px tanpa overflow/error console. `npm run build` dan `git diff --check` lulus. Data production read-only via `minipc-gudang-home` tetap identik: 246 stok, 241 `lokasi_id`, hash lokasi `952e1d953c348a08cd31ee22743e427e`; tidak ada schema/data yang diubah.
- **SELESAI — adopsi skill Claude ke Codex (Codex, 2026-08-01; commit `b424333`, DI-PUSH 2026-08-02).** Enam workflow project di `.claude/skills` dikonversi ke `.agents/skills`: `mulai`, `pendingitem`, `plan-warnoto`, `responsive-design`, `uimobile`, dan `update-data`; semuanya lulus validator `skill-creator` dan `git diff --check`. Tujuh skill desain generik tidak diadopsi karena bergantung pada tool/path Claude atau tumpang tindih dengan skill Codex global. Tidak ada source aplikasi, database, atau konfigurasi global yang diubah.
- **SELESAI — Perluas minQty computed-first ke Dashboard & bot Telegram, DI-COMMIT & DI-PUSH ke `main` (Claude, 2026-08-01, commit `ef40771`).** Lanjutan item di bawah (yang tadinya scope-nya dibatasi ke Forecast Stok saja) — user eksplisit minta diperluas. Root cause ditemukan: konsep "kritis" ternyata punya **3 implementasi terpisah** yang saling tidak sinkron — `getKritisAgg()` (`ragShared.mjs`, dipakai App.jsx 3x + `DashboardAsman`/`DashboardManager` + bot), `getMaterialAkanHabis()` (`analytics.js`, dipakai Dashboard juga, py `isKritis` sendiri dari minQty manual mentah), dan bot Telegram (`nightly_sync.mjs`). Kalau cuma `getKritisAgg` yang diperbaiki, Dashboard akan punya 2 definisi "kritis" berbeda. **Fix:** formula `computeEffectiveMinQty()` dipusatkan ke `ragShared.mjs` (file yang memang didesain Node+browser aman sejak awal, header filenya sendiri sudah wanti-wanti soal drift kalau logic disalin ganda) — dipakai `getKritisAgg`, `getMaterialAkanHabis`, dan `getRisk` (`ForecastStokPage.jsx`, sebelumnya salinan lokal `normInv`/`meanStdev` dihapus, sekarang import). `getKritisAgg(stocks, monthlySeriesByKatalogId={})` — param kedua opsional, backward compatible kalau ada caller yang belum di-update. Bot Telegram (`nightly_sync.mjs`) TIDAK query tabel baru — reuse `tug15_history` (180 hari terakhir) yang sudah ditarik untuk keperluan RAG chunk "mutasi", cuma limit dinaikkan 500→20000 (limit lama itu batas TOTAL lintas SEMUA katalog, berisiko terpotong diam-diam untuk warehouse dengan banyak material — jumlah baris riil sekarang di-`console.log` supaya kelihatan di log GitHub Actions kalau kepotong lagi). **Caveat penting:** bot cuma punya jendela 180 hari histori sedangkan Dashboard/Forecast Stok pakai SELURUH `txns` — jadi formula SAMA tapi angka minQty computed BISA beda antara bot dan browser untuk material yang pemakaiannya di luar 180 hari terakhir (bukan bug, keterbatasan data yang sudah ada di bot sebelum perubahan ini). Diverifikasi: `npm run build` lulus, unit test 36/38 (2 gagal `tugCanonical.contract.test.mjs`, dikonfirmasi PRE-EXISTING lewat `git stash` sebelum/sesudah — tidak terkait), simulasi node bandingkan formula lama vs baru identik 1:1 untuk 5 skenario, browser localhost:3001 dibandingkan stash/unstash — 0 perubahan visual di data produksi saat ini (belum ada material dengan histori ≥3 bulan). Test baru `tests/unit/minQtyEfektif.test.mjs` (5 test) ditambahkan permanen ke suite, bukan scratchpad.
- **SELESAI — Fix saldo TUG-15 minus + tab Rekomendasi Pengadaan (ROP/ROQ) + minQty otomatis dari histori (Forecast Stok saja), DI-COMMIT & DI-PUSH ke `main` (Claude, 2026-08-01, commit `79da8d2`/`4577924`).**
  1. **Fix saldo TUG-15 minus** (`src/lib/supabaseSync.js`, `buildMutasiRows()`): material "baru" (katalogMode!=="existing") dari TUG-3/TUG-10 dulu dapat katalogId sintetis ("-"/kosong), beda dari katalogId asli yang dipakai event keluar TUG-9/8 material yang sama → saldo berjalan per katalog kebagi 2 bucket, bisa minus (dilaporkan user: `LA;150KV;K;20KA;MOV;SM;104KV` tampil -6 padahal Data Stok/TUG-2 nyatanya 0). Fix: resolve katalogId lewat match nama ke `katalogList` (pola sama `buildKartuGantungHistory` di `sap.js` yang sudah benar).
  2. **Tab baru "Rekomendasi Pengadaan"** di Forecast Stok (switch 3 opsi: Forecast Stok / Rekomendasi Pengadaan / Material Cadang) — usulan qty beli untuk material Kritis+Waspada, 3 jalur: (a) sudah dianalisis Material Cadang → pakai `gapQty` Poisson yang ada; (b) ada histori pemakaian → ROP+ROQ baru (`reorder point = rata² pemakaian×lead time + Z×σ×√lead time`, lot order independen dari ROP, BUKAN turunan lagi seperti file referensi user 2022 yang secara aljabar `ROQ selalu = ROP` — itu akar masalah rekomendasi "beli 1" yang dikomplain user); (c) tanpa histori → restock ke stok minimum. Plus pagination tabel + tombol "Salin daftar" (clipboard, tanpa dependency baru) + tombol "Lihat detail" (reuse `openDetail` drill-down Forecast Stok yang sudah ada).
  3. **Stok minimum (`minQty`) sekarang computed-first**: kalau histori pemakaian ≥3 bulan, `minQty` efektif dihitung otomatis (reorder point service level 95%) dan dipakai untuk status Kritis/Waspada + formula (b) di atas; field manual "Min Qty Alert" (`StockModals.jsx`, TIDAK diubah) jadi fallback murni kalau histori belum cukup. Nilai turunan (useMemo), TIDAK ditulis balik ke `stocks[].minQty`/DB.
  **Scope sengaja dibatasi ke Forecast Stok saja** — Dashboard (Asman/Manager/`ExecOverview`) dan bot Telegram (`scripts/nightly_sync.mjs` via `getKritisAgg`/`ragShared.mjs`) MASIH pakai `minQty` manual apa adanya; `nightly_sync.mjs` tidak menarik data transaksi sama sekali jadi perluasan ke bot butuh kerjaan data-fetch terpisah. User eksplisit setuju "cukup di Forecast Stok dulu". Kalau nanti mau diperluas ke Dashboard/bot, itu keputusan baru, bukan otomatis lanjutan.
  Di data produksi saat ini (246 material, 35 Kritis, 0 Waspada) belum ada satupun material dengan histori ≥3 bulan, jadi klasifikasi Kritis/Waspada TIDAK berubah di data live — perubahan baru kepakai begitu histori transaksi cukup panjang. `npm run build` lulus tiap tahap, formula `normInv`/ROP/ROQ/minQty diverifikasi lewat simulasi node terpisah (bukan cuma laporan tukang, dicek ulang arsitek).
- **SELESAI — Ganti heuristik Forecast Stok dari rata-rata flat ke TSB (Teunter-Syntetos-Babai), DI-COMMIT & DI-PUSH ke `main` (Claude, 2026-08-01).** User pertanyakan rumus klasifikasi Kritis/Perhatian/Waspada/Aman setelah lihat 35 Kritis/0/0 — root cause diskusi: `getRisk()` lama pakai `total qty keluar ÷ jumlah bulan sejak transaksi tertua` (rata-rata flat), bias tinggi untuk pola pemakaian material gudang PLN yang intermiten/lumpy (berbulan-bulan 0 lalu keluar banyak sekaligus sekali waktu — persis kasus "233,333 M/bulan" yang dilaporkan user). **Fix:** `src/lib/tsbForecast.js` baru (`tsbMonthlyForecast`, `expandMonthlySeriesFromMap`, `buildMonthlyDemandSeries`) — smoothing eksponensial terpisah untuk probabilitas kejadian demand per bulan vs besaran saat terjadi (alpha=beta=0.1), forecast = probabilitas×besaran, otomatis meluruh ke 0 kalau material sudah lama tidak dipakai (beda dari Croston's klasik yang forecast-nya flat selamanya). Dipakai di 2 tempat, dibuat konsisten: `getRisk()` (`ForecastStokPage.jsx`, badge Kritis/dst di daftar & detail) dan `forecastDrillDown()` (`App.jsx`, prompt AI + fallback lokal — rata-rata pemakaian sekarang juga otomatis bulat, tidak perlu `Math.floor` manual lagi di titik pemakaian karena TSB sudah integer). 5 unit test baru (`tests/unit/tsbForecast.test.mjs`, node:test) lulus — cek forecast=0 kalau tak pernah ada demand, forecast lumpy jauh di bawah rata-rata flat, konvergensi ke steady-state demand teratur, dan pengisian bulan kosong. `npm run build` lulus, diverifikasi visual browser (localhost:3001, Forecast Stok: 246 material, 35 Kritis — semuanya short-circuit lewat cek `minQty>0 && stok≤minQty` dari 34 stok baru sesi sebelumnya jadi TIDAK terpengaruh perubahan TSB, itu memang benar; belum sempat verifikasi visual 1 contoh material Aman/live-transaksi yang before/after TSB-nya beda nyata — filter pill UI sempat tidak responsif saat dicoba, bukan indikasi bug kode), 0 console error saat fresh load. Hanya bagian "Analisis keputusan · Heuristik" yang berubah, panel "Model statistik · Prediksi ML Prophet" (`forecast_predictions`, model Python terpisah) TIDAK disentuh. **Sebelumnya** juga dibetulkan pembulatan rata-rata pemakaian di panel AI (`App.jsx:5157`/`:5200`, `Math.floor` — sekarang otomatis lewat TSB) atas laporan user yang sama. **PENDING ITEM (keputusan user, belum dikerjakan):** TSB saat ini dihitung di browser tiap kali data stok/transaksi berubah (via `useMemo`, bukan tiap render) — user tanya apakah ini bikin lemot, jawaban: TIDAK di skala sekarang (246 material, operasi aritmatika sub-milidetik), tapi user setuju idealnya dipindah ke job malam yang sama seperti Prophet (`ml/train_forecast.py`/tabel mirip `forecast_predictions`) supaya browser murni menampilkan hasil dan arsitekturnya konsisten dengan panel ML. User putuskan CUKUP DIBIARKAN DI BROWSER dulu (tidak ada masalah performa nyata), dicatat sebagai pending item untuk nanti, BUKAN dikerjakan sesi ini.
  **Bug susulan ditemukan & DIPERBAIKI sesi yang sama (`App.jsx`, commit terpisah):** user lapor panel AI drill-down material `TOOL M; COUNTER LA` (kode `7011491`, salah satu dari 34 stok baru sesi sebelumnya) tampil "Rata-rata pemakaian: 0/bulan" + "Estimasi stok habis: tidak dapat dihitung" TAPI teks analisis AI sendiri mengutip histori nyata "24 BH pada 2019-08" — kontradiktif. Root cause: `rataRataBulanan = Math.floor(forecastPerPeriod)` (fix pembulatan sesi sebelumnya) dipakai juga sebagai basis perhitungan `estimasiHari` (gate `rataRataBulanan>0`), bukan cuma untuk teks tampilan — material dengan demand TSB kecil (mis. 0,2/bulan, wajar untuk histori 1 kejadian di rentang 2019-2026) dibulatkan ke 0 lalu salah terbaca "tidak ada data sama sekali". **Fix:** `estimasiHari` sekarang pakai `forecastPerPeriod` mentah (bukan hasil floor/round), pembulatan `Math.round` (bukan `Math.floor` lagi) HANYA untuk `rataRataBulanan` yang ditampilkan di teks. Untuk material stok kosong dgn histori jarang, sekarang benar terhitung "0 hari" (=sudah habis) bukan lagi "tidak dapat dihitung". Dikonfirmasi ke user: rata-rata rendah + rekomendasi tetap restock BUKAN kontradiksi (logika safety stock untuk demand jarang tapi nyata), itu bagian yang sudah benar dari awal — cuma teks "tidak dapat dihitung"-nya yang salah. `npm run build` lulus, dicoba live browser (material yang sama, AI drill-down).
- **SELESAI — Import master katalog SAP + enrichment histori Forecast Stok dari `SAP AGUSTUS 2026.xlsx` (Claude, 2026-08-01, DIEKSEKUSI langsung ke DB production self-host).** Dua tahap dari file yang sama, user beri kriteria filter presisi tiap tahap. **Tahap 1 — Master Katalog** (sheet `SAP AGUSTUS`, laporan mutasi 1.750 baris → dedup 394 kode unik): 214 kode belum ada di `katalog`, user putuskan skip 7 kode berstatus `Val.Type=HAPUS` (write-off) → **207 kode baru diinsert** ke `katalog` (id `KAT-<kode>`, `jenisBarang` dari panjang kode/Val.Type sama pola `mapSAPRow`). Total katalog 220→427. **Tahap 2 — Histori Forecast** (sheet `mutasi material`, 1.750 baris transaksi per-event, filter `Tipe Per.` khusus {221,261,351}="pengeluaran" per instruksi user, 641 baris match): kode yang SUDAH ada di `stocks` (bukan cuma `katalog`) → semua histori pengeluarannya diimport (43 kode, 122 baris); kode yang BELUM ada di `stocks` tapi pengeluaran berulang >3x → histori diimport JUGA + dibuatkan baris `stocks` baru qty=0 (34 kode, 356 baris histori) — status "Kritis" TIDAK di-hardcode, cukup set `minQty=1` supaya rumus risk existing (`ForecastStokPage.jsx getRisk()`: `critical = minQty>0 && totalQty<=minQty`) otomatis menandainya; sisa 125 kode (pengeluaran ≤3x, tidak di stock) diabaikan sesuai instruksi. Histori masuk ke `legacy_history_archive` (`source_app='sap_mutasi_material'`, `jenis_transaksi='KELUAR'`, `source_upt='UPT Surabaya'`) — tabel yang sama dipakai `forecastDrillDown` (App.jsx, pola dari commit `390cad0`), jadi otomatis kebaca AI forecast tanpa ubah kode. **478 baris histori + 34 baris stok baru** ter-insert (`ON CONFLICT DO NOTHING`, tabel total 1279 baris). Ditemukan & ditangani 1 anomali format: katalog `4180001` tersimpan dengan leading-zero berlebih (`000000004180001`, entry manual lama beda pola dari seed asli) — no_katalog histori disesuaikan ke format persis itu supaya match `normalizeKatalogCode` tetap benar (dites eksplisit, 36 baris match). Semua tahap didahului **dry-run (`BEGIN...ROLLBACK`)** sebelum commit final, diverifikasi count + spot-check row per tahap. Tidak ada perubahan kode/skema — murni data production via psql langsung (SSH `minipc-gudang`).
- **SELESAI — Fix Pak War/Telegram "jadi bodoh" + Forecast Stok kosong + kuota Cohere boros, DI-COMMIT ke `main` (Claude, 2026-07-30, `f79195f`+`7afc2c5`+`2b508f7`; Groq key sudah di-deploy Vercel duluan).** User lapor 3 hal: Pak War mati, Forecast Stok mati, lalu Cohere 429 "Trial key limited to 1000 API calls/month" muncul di menu sinkron. **Root cause #1 (Groq):** `VITE_GROQ_API_KEY` (client, beda dari `GROQ_API_KEY` server yang sudah diperbaiki 2026-07-29 untuk Telegram) sudah expired 401 — Vercel Production malah kosong total. Fix: pakai ulang key server yang sudah terbukti valid (dites langsung ke Groq API, HTTP 200), pasang ke `.env`/`.env.local`/`.env.staging` + Vercel (production/preview/development), redeploy (`warnoto-o0gcceq7d`). **Root cause #2 (Forecast Stok):** bukan bug — RBAC & DB connection semua sehat (dicek langsung), tapi `tug15_history` cuma 10 baris KELUAR total (maks 2/katalog), jauh di bawah `MIN_DATA_POINTS=10` di `ml/train_forecast.py`. Fix: tambah `fetch_legacy_history()` menarik `legacy_history_archive` (UPT Surabaya, 516 baris KELUAR, **30 cocok** katalog WARNOTO via `normalize_katalog_code` — sisanya diabaikan sesuai instruksi user), gabung ke histori live; turunkan `MIN_DATA_POINTS` ke **5** (keputusan user, karena bahkan digabung histori terbanyak cuma 5 baris) — tetap perlu dinaikkan lagi nanti setelah histori live bertambah. **Root cause #3 (Cohere 429, penyebab sebenarnya Pak War "jadi bodoh"):** BUKAN dari job malam (`nightly_sync.mjs`, sudah "success" terus per GH Actions) — ternyata dari `syncRagChunks()` di `App.jsx`, dipanggil auto (debounce 90 detik tiap ada perubahan stocks/txns) DAN tombol manual, yang SELALU re-embed SEMUA chunk tanpa cek perubahan sama sekali — jauh lebih sering jalan daripada cron malam, jadi sumber boros quota Cohere trial (1000 call/bulan) yang dominan. Fix: fungsi `splitChunksForEmbed()` dipusatkan di `src/lib/ragShared.mjs` (dipakai browser DAN `nightly_sync.mjs`, bukan file terpisah per-runtime) — chunk yang `content`-nya identik dengan yang tersimpan di `rag_chunks` di-skip total, cuma yang baru/berubah dipanggil ke Cohere. Ditambah jadwal `nightly-knowledge-sync.yml` diubah dari tiap malam jadi **tiap 3 hari** sebagai headroom ekstra. **Verifikasi:** RLS aktif di semua tabel sensitif (`katalog`/`stocks`/`rag_chunks`/`forecast_predictions`/`warnoto_state`/`tug_transactions`/`profiles`/`role_permissions`), client cuma pakai `anon` key (bukan `service_role`), TLS `warnoto.com` OK. `rag_chunks` saat ini: 220 katalog + 7 mutasi (segar dari sync sukses terakhir), **0 txn/faq** (belum sempat sinkron sejak Cohere quota habis — akan terisi begitu app dibuka/auto-sync jalan lagi). `forecast_predictions` masih 0 baris (menunggu job `train_forecast.py` jalan dengan kode baru — jadwal malam berikutnya atau trigger manual). Semua self-test (`nightly_sync.selftest.mjs`, `train_forecast_selftest.py`) lulus, `npm run build` lulus. **Efek samping ditemukan & DIABAIKAN atas keputusan user:** `.env.staging` menunjuk ke Supabase Cloud lama — TERNYATA disengaja (sandbox staging terpisah dari data production, ada di komentar file), bukan bug, TIDAK diubah.
- **Bonus dari sesi yang sama — script migrasi Non-Stock reusable (`2b508f7`).** `migration-tools/match_nonstock_mara.py`: cocokkan material Non-Stock AppSheet lama ke katalog MARA (kode persis dulu, fallback keyword overlap), filter per-UPT + qty, exclude yang sudah ada di katalog WARNOTO (livecheck dari DB). Output review-first ke Excel (`outputs/warnoto-nonstock-review/`, gitignored), TIDAK menulis ke Supabase. Untuk UPT Surabaya: dari 624 baris, 110 baris qty>0 diusulkan (14 kode persis/29 kuat/66 lemah/1 tanpa kandidat) + 300 baris qty=0/kosong di sheet review terpisah — user masih perlu cek manual sebelum eksekusi ke Supabase.
- **SELESAI — Template dokumen TUG-9 4-halaman dari PR eksternal #7 kontributor Kevin (Claude, 2026-07-30).** PR "Template TUG 9" (`feature/tug9-template-presisi`) berisi 2 commit campur; hanya di-cherry-pick SEBAGIAN setelah review arsitek. **Diambil:** rewrite `buildTUG9HTML` di `src/lib/docBuilders.js` — format 4 halaman meniru AppSheet lama (hal 1: Surat Jalan + Berita Acara Serah Terima Barang/BAST-B dalam satu halaman, hal 2-4: lampiran foto kendaraan/SIM-KTP, foto surat, foto per-material dalam tabel); tombol dokumen di `TransactionHubTab.jsx` diubah dari hanya tampil saat `status==="APPROVED"` jadi selalu tampil (label "Lihat & Pratinjau Dokumen") supaya bisa pratinjau sebelum approve. Arsitek juga membuang 2 buah `<a href="#">` dead-link di sekitar nomor dokumen (peninggalan template asli, tidak fungsional). **DITOLAK, TIDAK diambil sama sekali:** commit kedua PR (`fix(tug9): fallback TUG-8/9 transaction storage to local storage when Supabase canonical is unavailable`) — commit ini menghapus guard `CANONICAL_TUG_REQUIRED` di `App.jsx` dan bikin approval TUG-8/9 diam-diam fallback ke local storage kalau Supabase gagal, persis pola bug "data-loss senyap" yang baru saja diperbaiki kemarin di semua editor Master Data (lihat entri di bawah). Juga tombol dummy "🧪 Isi Data Contoh" di `TugFormModals.jsx` (isi form produksi dengan data palsu) tidak diikutkan — risiko kepencet user asli di production. `App.jsx` dan `TugFormModals.jsx` dikonfirmasi nol perubahan (`git diff` kosong). Diverifikasi: `npm run build` lulus, tombol preview + render dokumen dicoba live di browser lokal (tersambung ke production self-host `warnoto.com`) — semua foto (kendaraan/SIM-KTP signed URL/surat/material) fetch HTTP 200, tidak ada console error. User konfirmasi visual "TUG 9 sudah bagus". PR #7 GitHub belum ditindaklanjuti (belum di-comment/close).
- **SELESAI — DI-MERGE ke `main` & PRODUCTION, SEMUA VERIFIKASI LOLOS (Claude, 2026-07-30).** User uji coba TUG-9 di production self-host (akun Admin "Fajar"): approval TL tampil tapi lambat + trail "PREPARED (PENDING_TL)" berulang 6x + menampilkan hash/JSON mentah yang membingungkan; approval final Asman GAGAL total (macet, tidak sampai PDF); setelah itu tombol Tolak juga tidak menghilangkan kartu/notifikasi approval. **Root cause 1 (ditemukan & diperbaiki):** `TugFinalReviewModal.jsx` `useEffect` bergantung ke `[txn, prepareReview]` yang identitasnya berubah tiap re-render `ApprovalTab` (arrow inline + `txn` dari `.map()` state) — RPC `tug_prepare_review` (menulis token + event PREPARED baru TIAP panggil, tanpa idempotency) jadi terpanggil berulang selama modal terbuka, token/hash berubah-ubah, approval final Asman ditolak server (`TUG_REVIEW_REQUIRED`/`STALE`). Fix: deps jadi `[txn.id]` saja (1x per sesi modal). **Root cause 2 (ditemukan & diperbaiki):** `tug_transactions` TIDAK punya realtime subscription (beda dari `stocks`) dan cuma di-load sekali saat login — kalau approver lain approve di sesi/tab lain, versi lokal basi, approve/reject berikutnya ditolak server (`TUG_VERSION_MISMATCH`) sementara UI tetap menampilkan kartu seolah pending (ini penyebab bug "tombol Tolak tidak hilang"). Fix: refetch `loadCanonicalTugTransactions()` tiap tab Approval dibuka. **Temuan tambahan (diperbaiki):** Mode Demo (`isDemoMode()`) TIDAK PERNAH dicek oleh jalur canonical TUG-8/9 (`tugCanonical.js`) — transaksi "uji coba" di Mode Demo sebenarnya menulis ke server sungguhan, bertentangan dengan jaminan Mode Demo. Fix: blokir pembuatan TUG-8/9 saat Mode Demo aktif (toast error, pola sama dengan larangan Mode Demo lain di `App.jsx`). **Keputusan produk (dieksekusi):** jenjang Asman disederhanakan — cukup ringkasan dokumen + tombol Setuju/Tolak langsung (tanpa hash/JSON/checklist 4-attestation), review lengkap dengan checklist tetap wajib di level TL saja (`asmanStage` di `TugFinalReviewModal.jsx`); attestation tetap dikirim penuh (`ASMAN_ATT`, semua true) ke server karena `tug_decide` mewajibkannya untuk `FINAL_APPROVED` (SQL tidak disentuh). **Cleanup:** transaksi uji lama `227.TUG-9/LOG.00.02/SBYA/VII/2026` (id `38c53970-71f0-4260-9172-4c02dd265c89`) dihapus manual dari self-host (SSH `minipc-gudang-home` + psql, hapus `tug_review_tokens`/`tug_approvals`/`tug_items`/`tug_transactions`) — terkonfirmasi `stock_movements: 0` untuk txn ini, jadi TIDAK PERNAH benar-benar mengubah stok nyata meski sempat macet 12 token/15 event approval menumpuk. **BELUM diverifikasi end-to-end** (buat TUG-9 baru → approve TL → approve Asman → PDF final muncul → qty Data Stok berkurang & tersinkron setelah reload) — build lokal lulus tapi tidak ada uji coba nyata di server (localhost tersambung ke production self-host, tidak ada sandbox aman; Mode Demo sekarang memang sengaja memblokir TUG-8/9 jadi tidak bisa dipakai untuk uji coba approval). **DI-MERGE ke `main` & DI-PUSH ke Vercel (2026-07-30, `c8209f8`, atas instruksi eksplisit user "langsung" — SEBELUM konfirmasi hasil uji end-to-end TL→Asman→PDF→Data Stok selesai dilaporkan balik ke arsitek.** Selama sesi uji coba, ditemukan & diperbaiki di branch yang sama (semua ikut ke-merge): (1) preview dokumen final JSON mentah → tampilan terbaca (grid identitas + tabel material bernama, `de6d5b2`); (2) trail approval disederhanakan (dedupe event berturut-turut + hitungan) & hash dokumen disembunyikan dari reviewer (`eda7bb2`); (3) dev server `npm run dev` gagal total lewat proxy `/supabase` karena Node tidak baca trust store Windows (laptop dev ternyata kena SSL inspection kantor juga) — fix `--use-system-ca` di `package.json` (`2900d18`); (4) bug data-loss SENYAP di SEMUA editor Master Data (Satpam, Tim Mutu, UIT, ULTG, UPT, Gudang, Lokasi, Sub Gudang) — `syncMasterTable`/`syncLokasi`/dst mengembalikan `false` saat gagal tapi caller tidak pernah cek, state lokal+cache+audit+toast sukses tetap jalan seolah tersimpan padahal server tidak menerima; fix rollback state + toast error jujur di semua titik (`3e323fb`, `d2b5a3d`); (5) "Riwayat Approval" pakai UUID mentah bukan nomor dokumen (`2ca92f3`); (6) tabel Laporan TUG-15 ascending (lama→baru) di layar, transaksi baru selalu di bawah — dibalik utk tampilan layar saja, export PDF/Excel tetap kronologis (`2ca92f3`); (7) baseline "Migrasi Data" ditambahkan ke Laporan TUG-15 (pola sama `buildKartuGantungHistory` di `sap.js`, hitung mundur dari qty stok sekarang supaya tidak dobel-hitung) supaya "Total Masuk" material tanpa arsip legacy tidak selalu 0 (`4162e22`) — catatan: baris ini `ts:0` jadi ikut tersaring kalau filter tanggal aktif, sama seperti arsip lama. **VERIFIKASI END-TO-END SELESAI (2026-07-30, sesudah merge, langsung di production):** transaksi nyata `228.TUG-9/LOG.00.02/SBYA/VII/2026` (id `a64bfcfb-fb71-4356-afcb-3e41971886ad`) — TL Widi Ferdian R approve 07:33:00 → Asman Rouuf approve final 07:33:31 → status `FINAL_APPROVED`. Dicek langsung ke `stock_movements`: 1 baris `direction:OUT qty:3 before_qty:6 after_qty:3` terikat `transaction_id`+`stock_id` spesifik (bukan operasi massal — RPC `tug_decide` di `20260729_tug_canonical_approval.sql:500` pakai `UPDATE ... WHERE id=i.stock_id` di dalam loop per-item), cocok persis dengan qty `stocks` sekarang (3) dan dengan Saldo Awal/Akhir di Laporan TUG-15. Trail approval sempat ~20 event PREPARED berturut-turut (modal review dibuka-tutup berkali-kali selama sesi uji coba) — tidak masalah karena sudah dirangkum UI (fix `eda7bb2`), bukan indikasi bug baru. Fitur Laporan (nomor dokumen, urutan terbaru-dulu, baseline Migrasi Data) dan Master Data (Satpam/UIT-ULTG/Gudang, alur simple maupun multi-sync) dicoba langsung di production (tambah/edit → cek DB langsung via SSH → revert) — semua tersimpan benar. **Matrix Izin (`PermMatrixPage.jsx`) dicek terpisah**: kode ini SUDAH aman dari awal (beda dari bug Master Data lain) — `saveAll()`/`resetRole()` sudah mengecek `error` dari `supabase.upsert()` dan toast jujur kalau gagal, upsert per-role satu-satu (bukan reconcile-delete tabel penuh); live-test toggle `menu.dashboard` role VIEWER → `updated_at` berubah di DB → revert → `updated_at` berubah lagi, keduanya tercermin benar. **Tidak ada temuan terbuka tersisa dari sesi ini.**
- **SELESAI — Kartu Gantung TUG.2 (Claude, 2026-07-29, `9214088` di `main`, di-push ke Vercel).** Fitur cetak Kartu Depan (foto+QR) & Riwayat Keluar-Masuk di-cherry-pick dari PR kontributor (`da0732f`, hanya commit itu — sisa PR #6 sengaja TIDAK diambil karena bundel Inspeksi Material Cadang/TUG3/TUG5/SignaturePad/dev-login-bypass yang tidak diminta). Dibenahi: foto barang disambungkan ke `fotoKeseluruhan` Storage (bukan field `img` lama), "KARTU No." diganti kategori material (`jenisBarang`), kolom tabel Riwayat direstruktur (TGL/NO.BON/MASUK/KELUAR/RAK/LOKASI/JUMLAH/CATATAN, hapus grup SISA PERSEDIAAN & kolom PETI). **Keputusan penting:** Riwayat TIDAK menggabung arsip legacy (`legacy_history_archive`) — sempat dicoba lalu DIBATALKAN user karena status data beberapa material belum konsisten di arsip lama; sekarang cukup 1 baris baseline "Migrasi Data" dengan JUMLAH = qty Data Stok saat ini, transaksi TUG baru dari aplikasi ini yang menambah baris berikutnya. Header "Lokasi :" tampil lengkap Gudang+SubGudang+Blok; kolom LOKASI di tabel Riwayat pakai singkatan Sub Gudang (`subGudangKodeMap`, reuse dari Master Data) supaya compact. Build lulus, diverifikasi visual browser tiap tahap. PR #6 asli tidak di-merge/di-close (belum ada tindakan lanjutan, masih terbuka di GitHub).
- **SELESAI — rollout TUG canonical ke self-host dan production (Codex, 2026-07-29; `59c4f23` + `69d42c7`).** Migrasi `20260729_tug_canonical_approval.sql` diterapkan idempoten dalam single transaction setelah backup production valid. Uji produksi ber-rollback membuktikan Admin create/submit → TL approve tanpa perubahan stok → overview Asman → final approve menurunkan stok 5→3 tepat sekali; rollback meninggalkan 0 transaksi/item/approval/review/movement/idempotency dan counter kembali 225. Baseline setelah uji tetap: 212 stok (total qty 37.077,5), 220 katalog, 106 lokasi, 3 gudang, 801 arsip legacy, 401 dokumen legacy, 8 riwayat TUG-15, serta `wa_sync_status` tetap 1. Verifikasi: PostgreSQL scratch rehearsal, 17/17 contract test TUG+Telegram, Playwright TUG 2/2, build, `git diff --check`, graphify, Vercel success, dan bundle production memuat RPC create/review canonical.
- **SELESAI — pemulihan Telegram (Codex, 2026-07-29; `69d42c7`).** Root cause jawaban AI gagal adalah `GROQ_API_KEY` lama kedaluwarsa (401), sedangkan webhook dan whitelist tetap sehat. Key baru dipasang hanya di `/home/admin_warnoto/vps-dr-stack/.env`, service `functions` saja yang direcreate, dan backup `.env` dibuat sebelum perubahan. Groq model list + chat completion HTTP 200, container Edge Functions `running/healthy`, webhook `https://warnoto.com/functions/v1/telegram-webhook` HTTP 200, knowledge base `wa_sync_status` tetap `OK`/227 record. Fallback malformed Markdown sudah aktif dan diuji 2/2; tidak ada pesan uji yang dikirim ke pengguna Telegram.
- **SELESAI — Add/Edit lengkap dan upload foto Alat Berat untuk Admin Gudang (Codex, 2026-07-29).** Admin dapat menambah alat dan mengubah seluruh field; TL tetap dibatasi ke foto/status. Foto baru masuk Storage `tug-photos/alat-berat/`, sedangkan data alat memakai upsert satu baris ke tabel self-host `heavy_equipment`. Format dibatasi ke JPG/JPEG, PNG, dan WebP; HEIC ditolak sebelum diproses. Error validasi/pemrosesan foto, upload Storage, dan sinkronisasi dipisahkan; modal, foto, dan seluruh isian tetap terbuka bila gagal. Tabel production terverifikasi berisi 51 baris tanpa data test. Verifikasi: Playwright UI 7/7 (termasuk unsupported, upload gagal, form tetap utuh, dan update foto valid), unit test gabungan 11/11, build, REST self-host HTTP 200, `git diff --check`, dan graphify lulus.
- **SELESAI — aturan review lokasi Data Stok (Codex, 2026-07-29).** Seluruh 207 lokasi migrasi/SAP existing dinyatakan valid oleh user dan tidak dimutasi. Panel Admin `Review Lokasi Otomatis` beserta aksi `Pertahankan/Kosongkan` dihapus. Perubahan dalam Gudang yang sama langsung tersimpan; perpindahan beda Gudang oleh Admin masuk approval TL dan baru mengubah `gudangId`+`lokasiId` setelah disetujui. Unit test alur request/approve/reject lulus.
- **SELESAI — menu TUG/Laporan TUG-15 (Codex, 2026-07-29).** TUG-15 kini langsung berupa banner tanpa selector satu-tombol; status filter yang tidak relevan disembunyikan khusus Laporan; filter tanggal dan pencarian bekerja gabungan; drawer riwayat menjadi modal tengah dan material tanpa katalog tidak tercampur. Tombol unduh berada di bawah Cari. PDF asli berupa dashboard ringkasan pemakaian bergaya Power BI dan dibatasi tepat 2 halaman; Excel mengikuti format Monitoring Persediaan PLN dan kolom Lokasi Gudang hanya memakai nama gudang utama dari relasi master (arsip legacy tanpa relasi memakai nama `GUDANG ...` eksplisit atau fallback UPT). **Interaksi final:** klik seluruh baris (kecuali tombol Riwayat lengkap) membuka detail satu transaksi/baris saja; tombol **Riwayat lengkap** tetap membuka seluruh timeline material yang sama. Timeline lengkap memiliki filter Semua/Masuk/Keluar, header Masuk hijau dan Keluar merah, serta badge `Lama` di kanan bawah tanggal. Kedua mode memakai `materialKey`, sehingga material lain tidak tercampur. Verifikasi akhir terbaru: Playwright TUG-15 desktop+mobile **22/22**, unduhan PDF/Excel asli, audit visual modal+dua halaman PDF, build, `git diff --check`, dan graphify lulus.
- **PR #5 "Update TUG" tidak boleh di-merge mentah dan kini tersupersede sebagian oleh implementasi canonical di `main` (`59c4f23` + `69d42c7`).** Risiko lama tetap berlaku: branch PR berasal dari TUG-15 sebelum arsip legacy dan akan menghapus fitur Laporan bila digabung langsung. Jika PR masih terbuka, lakukan audit/close terpisah; jangan mengambil `TUG15Tab.jsx` atau menimpa alur canonical yang sudah live. Bagian dokumen/signature yang belum masuk hanya boleh dipilih manual setelah dibandingkan dengan keputusan TUG canonical dan kondisi `main`.
- **Pipeline arsip migrasi history AppSheet lama (Claude, 2026-07-28, DI-COMMIT `e62f4cc`, DIEKSEKUSI ke production — lihat detail di bawah).** User minta history transaksi TUG3/4/5/8/9/10 dari aplikasi WARNOTO versi lama (AppSheet, DB Excel) bisa dilihat/didownload di WARNOTO baru sebagai arsip — TANPA masuk ke transaksi material aktif (format katalog lama vs baru beda) — dan diminta dibakukan strukturnya dulu (bukan langsung eksekusi import) supaya UPT lain bisa reuse pipeline yang sama nanti. **Ditemukan pekerjaan cleansing sudah pernah dikerjakan sebelumnya (2026-07-04, tidak tercatat di HANDOFF saat itu):** `migration-tools/clean_warnoto_history.py` (Excel AppSheet → JSON ternormalisasi) sudah ada duluan.
  **Dibangun/diperluas sesi ini:** (1) tabel `legacy_history_archive` (`supabase/schema.sql` section 26) — tanpa FK ke `katalog`, RLS read `authenticated`, tulis cuma service_role; (2) `migration-tools/load_legacy_history_to_supabase.mjs` — loader generik upsert per `sync_key`, dukung `--dry-run`/`--upt`; (3) perluasan `clean_warnoto_history.py` menangkap lampiran dokumen (foto surat jalan/SIM-KTP sopir/kendaraan, PDF Bon/Berita Acara/Lampiran — key JSON baru `dokumen_header`, + `foto_barang_relpath` per item) + tabel `legacy_history_documents` (section 27, kolom `foto_surat_jalan_url`/`foto_sim_ktp_url`/`foto_kendaraan_url`/`pdf_url`/`berita_acara_url`/`lampiran_url`) — SIM/KTP + semua PDF ke bucket privat `tug-docs-private` (prefix `priv:`, data pribadi), surat jalan/kendaraan/foto barang ke bucket publik `tug-photos`; (4) `migration-tools/upload_legacy_history_attachments.mjs` — mencocokkan nama file dari Excel ke file fisik hasil ekstrak backup AppSheet lama (`D:\CLAUDE\WARNOTO data\Appsheet\_extracted\data\`), resolusi tabrakan nama file via hash SHA-1 (identik = aman dipakai, beda isi = ditandai "AMBIGU" di kolom `match_notes` untuk review manual — belum pernah kena kasus nyata, cuma duplikat identik yang muncul); (5) `migration-tools/README.md` — dokumentasi pipeline 5 langkah reusable untuk UPT manapun.
  **Sumber data final** (dikoreksi user pertengahan sesi): `D:\CLAUDE\WARNOTO data\Appsheet\DB_Warnoto Juli 2026.xlsx` (BUKAN file 2026-07-04 yang lebih lama) — sudah diproses ulang lewat pipeline lengkap (termasuk pandas yang perlu di-`pip install` dulu di environment ini, sudah beres). Hasil: **2025 baris transaksi** (breakdown UPT: Gresik 1157, Surabaya 801, Probolinggo 37, Malang 21, Bali 5), **860 dokumen header** dengan lampiran (match-rate lampiran ~93-94% untuk UPT Surabaya, 0 kasus ambigu file). Output JSON terbaru: `outputs/warnoto-history-clean/cleaned_history_juli2026.json` (belum di-commit — cek relevansinya dengan `.gitignore`, file data besar).
  **Cakupan yang disetujui user: UPT Surabaya dulu saja** (801 baris + lampirannya), UPT lain menyusul terpisah sesuai kebutuhan.
  **DIEKSEKUSI ke Supabase produksi (Claude, 2026-07-28, dikonfirmasi user).** `load_legacy_history_to_supabase.mjs` (801 baris UPT Surabaya, upsert on_conflict=sync_key) + `upload_legacy_history_attachments.mjs` (401 dokumen header + 784 foto barang, upload ke bucket `tug-docs-private`/`tug-photos`) sukses jalan (`=== Selesai ===`, exit 0, tidak ada baris "Gagal upsert/update"). Env var `SUPABASE_URL`/`SUPABASE_SECRET_KEY` diisi dari `NEW_SUPABASE_URL`/`NEW_SUPABASE_SECRET_KEY` yang SUDAH ADA di `.env` lokal (tidak perlu SSH). Data lama (dari proses migrasi 2026-07-04 yang kurang lengkap, tanggal kosong & lampiran minim) tergantikan oleh dataset ini via upsert. Match-rate lampiran per jenis (dari dry-run): surat_jalan/sim_ktp/kendaraan/lampiran ~90%, PDF ~93%, berita_acara ~75% (banyak dokumen memang tidak selalu punya BA), 0 kasus ambigu. **UPT lain (Gresik/Probolinggo/Malang/Bali) BELUM dieksekusi**, menyusul terpisah sesuai kebutuhan.
  **Mockup UI disetujui user secara visual, BELUM diimplementasikan ke React sama sekali.** Rencana tampilan tab Laporan Mutasi Stok (`TUG15Tab.jsx`): baris arsip "Lama" tampil berdampingan dengan transaksi "Baru" (badge Sumber + filter pill), kotak pencarian riwayat barang lintas sumber (nama/kode katalog/no dokumen/UPT, TIDAK terikat filter tanggal), dan tombol "Riwayat lengkap" per barang yang membuka panel/drawer timeline gabungan Baru+Lama (total masuk/keluar, rentang tanggal, lini masa kronologis, lampiran terkait). Gaya visual sudah diarahkan user jadi korporat/profesional: top bar navy + wordmark, ikon SVG line (bukan emoji), badge pill tanpa simbol dekoratif, memakai token warna asli `src/theme.js` (bukan palet baru). **Artifact mockup TIDAK disimpan URL-nya di sini** (sesi lain/vendor lain tidak bisa akses artifact milik sesi ini) — kalau perlu rujukan visual, minta user share ulang linknya atau desain ulang cepat berdasarkan deskripsi di atas.
- **Fix bug "Koneksi realtime Data Stok terputus" / tabel `stocks` bengkak 119KB→12MB — root cause ditemukan & DIPERBAIKI, DI-COMMIT & DI-PUSH ke `main` (`ff7fd49`, 2026-07-28, deploy Vercel otomatis).** User lapor app terasa down + banner "Koneksi realtime Data Stok terputus" menggantung. Investigasi: tunnel & container sehat, tapi log Kong menunjukkan `GET /rest/v1/stocks?select=*` 12.7MB (baseline normal 119KB, insiden SEKELAS 2026-07-23) — 4 baris `stocks` kena foto base64 mentah ter-embed lagi di `fotoKeseluruhan` (5.9MB/3.2MB/2.8MB/271KB). Client Realtime melakukan snapshot REST saat reconnect (fitur `09001e9`) — fetch besar ini yang bikin reconnect gagal/lama. **Root cause kode** (kenapa berulang): ada 5 titik yang menyimpan foto LANGSUNG sebagai base64 ke DB tanpa pernah upload ke Supabase Storage (beda dari pola benar TUG txn photos) — `updateStockFoto` (tombol Simpan Foto detail Data Stok), `saveStock` (modal Tambah/Edit Data Stok), `addNonStockFoundItem` (material baru saat Stock Opname Non-SAP, table `stocks`), item mirror di `StockOpnameTab.jsx` (table `stock_opname`), `saveHeavyEquipmentEdit` (foto Alat Berat, table `heavy_equipment` — ditemukan saat audit menyeluruh, belum ada insiden nyata tapi pola identik). **Fix** (tukang-opus untuk 2 titik utama Data Stok, arsitek langsung untuk 3 titik lain — root cause & pola sudah pasti dari analisa sendiri): semua sekarang kompres (`compressImage`) + upload ke Supabase Storage dulu (bucket `stock-photos` untuk Data Stok/Opname path `upt-surabaya/<katalog>/utama.jpg`, reuse bucket `tug-photos/alat-berat/<id>.jpg` untuk Alat Berat — belum ada bucket khusus), baru field diisi URL; kalau upload gagal → toast error + BATALKAN simpan (tidak ada lagi fallback diam-diam ke base64). `_uploadTxnPhoto` (supabaseSync.js) di-export & rename `uploadPhotoToStorage` supaya reusable lintas TUG/Data Stok/Alat Berat. **Migrasi data production**: 4 baris `stocks` yang sudah kena diupload manual ke Storage via SSH (arsitek langsung, service_role key dari user — operasi ini diblokir classifier untuk Agent/delegasi otomatis, konsisten dengan kebijakan insiden 2026-07-23) — `stocks` kembali ke 173KB, diverifikasi HTTP 200 untuk foto baru. `npm run build` lulus, `git diff --check` bersih, `graphify update .` dijalankan. **Belum diverifikasi visual browser** untuk upload foto baru pasca-fix (Data Stok/Opname/Alat Berat) — perlu dicoba user di production. **Temuan sampingan tidak terkait**: sempat ada laporan CORS/"server tidak terhubung" saat login dari HP+tethering setelahnya — dicek server-side (DNS/tunnel/auth semua sehat dari luar, DNS `warnoto.com` juga resolve benar di 3 resolver publik independen) — user konfirmasi sudah pulih sendiri, kemungkinan blip DNS resolver operator seluler (domain masih baru), BUKAN regresi dari fix ini.
- **Realtime Data Stok production (Codex, DI-COMMIT & DI-PUSH `09001e9`, 2026-07-27):** Vercel deploy sukses; container Realtime healthy dan dua replication slot aktif. Uji terisolasi `TEST-REALTIME-20260727-09001E9` membuktikan INSERT, UPDATE, dan DELETE langsung terlihat pada dua sesi browser tanpa refresh dan tanpa console error; row test sudah dihapus dengan final count `0`. Unit test khusus 8/8, production build, graphify, dan diff check lulus.
- **Revisi Inspeksi Material — TAHAP BACKEND SELESAI, BELUM commit/push/DDL (Claude/tukang-opus, 2026-07-27):** Implementasi multi-material per BA sesuai rancangan disetujui (poin 2b). File berubah: `supabase/schema.sql` section 20b (tabel `material_inspection_seq` + `material_inspection_batches` parent + `batch_id` pada `material_inspections` + RPC `create_material_inspection_batch(p_items,p_header)` security-definer atomik generate nomor `000001/BA-INSPEKSI/UPT-SBY/MM/YYYY` + migrasi idempoten v1→batch-satu-item + `set not null`), `src/lib/materialInspectionSync.js` (`createMaterialInspectionBatch` upload-cleanup + `loadMaterialInspectionBatches`; fungsi v1 ditandai `LEGACY`), `App.jsx` (state `materialInspectionBatches` + load + props), `tests/material-inspection-contract.test.mjs` (+5 test). Verifikasi arsitek independen: `npm run build` lulus (18.77s), 9/9 contract test lulus, `git diff --check` bersih, wiring App.jsx aman. **DDL BELUM dijalankan ke production, BELUM commit/push.** Riwayat inspeksi lama akan direnumbering (v1 tak simpan nomor BA) — perlu disampaikan ke user. **RISIKO ORDERING (belum teratasi):** DDL drop insert-policy pada `material_inspections` akan membuat UI v1 yang masih aktif (`createMaterialInspection`) gagal RLS saat baru jalan. Rollout HARUS atomik dengan switch UI baru (tukang-sonnet, task #2), atau pertahankan insert-policy lama sementara + drop di follow-up. Keputusan rollout menunggu user.
- **Revisi Inspeksi Material — TAHAP UI SELESAI, BELUM commit/push/DDL (Claude/tukang-sonnet + arsitek, 2026-07-27):** Rewrite `src/components/InspeksiMaterialCadangTab.jsx` (182→576 baris): switch `Buat Inspeksi | History BA` (pola Maturity navy gradient + `isMobile` flexDirection); header read-only otomatis (Tanggal hari ini Asia/Jakarta, UPT=UPT-SBY, Gudang terkunci dari material pertama, Nomor BA "(terisi otomatis saat simpan)", Manager=Yaya Supriman hardcode); input manual hanya Pelaksana Logistik (default `currentUser.name`) + Pelaksara Pemeliharaan (wajib, typo v1 "pelaksara" dipertahankan untuk kompat data lama). Form multi-material: picker stok Cadang canonical (`katalog.jenisBarang==="Cadang"`, TANPA input manual), identitas terkunci read-only + ganti lewat Remove, maks 10 material, tiap item Kondisi/Kelayakan/JenisMTU/KeteranganVisual/Catatan/Checklist 4-checkbox/2 foto wajib (`usePhotoPreviews` hook revoke object URL anti-leak, `ponytail:` comment). History: card per BA (grid auto-fit desktop, 1 kolom mobile, NO tabel horizontal), expand detail + foto signed URL, `printBa(batch)` multi-material tabel multi-baris (`@media print` pola v1 `.inspection-ba`). Wiring App.jsx: tambah `isMobile={isMobile}` saja. Verifikasi arsitek independen: `npm run build` lulus (25.19s/4835 modules), `git diff --check` bersih. Test: 1 test v1 ("one inspection retains its own final BA", `finalBa:`/`printBa(inspection)`) sah patah karena kontrak UI berubah — **arsitek update test ke kontrak v2** (`nomorBa`/`printBa(batch)`/`pelaksaraPemeliharaan`, judul jadi "batch UI carries BA header...") — mekanis, 1 test, bukan keputusan skema. **9/9 contract test lulus.** **ROLLOUT ATOMIK SELESAI (Claude, 2026-07-27, DI-COMMIT & DI-PUSH ke `main` — `8f611d8`):** user approve rollout; DDL section 20b dijalankan LEBIH DULU ke production (`ssh minipc-gudang` + `docker exec supabase-db psql --single-transaction`, idempoten) — 0 baris `material_inspections` existing jadi migrasi v1→batch tidak menyentuh data apa pun; diverifikasi tabel `material_inspection_seq`/`material_inspection_batches` baru, RPC `create_material_inspection_batch` ada, bucket privat `material-inspection-photos` (`public=false`) — BARU SETELAH itu commit+push kode (tidak ada jendela UI lama gagal RLS). `npm run build` lulus ulang sebelum commit.
- **Redesain UI mobile `/uimobile` (Codex, DI-COMMIT & DI-PUSH `1f8c5aa`, 2026-07-27):** Dashboard Summary + Overview Default dipadatkan dengan ikon Phosphor dan progressive disclosure; filter jenis Alat Berat dirapikan vertikal; Data Stok menjadi kartu compact dengan pencarian keterangan/lokasi, pencarian foto tetap aktif, klik kartu membuka detail, serta rail aksi ringkas Lokasi + Kartu Gantung. Workflow lokal `responsive-design` + `/uimobile` ikut dipasang. Verifikasi build, graphify, diff check, snapshot 360/390/412/768, focused mobile 8/8, dan desktop Data Stok 1/1 lulus. ATTB tidak disentuh; parity Dashboard Asman/Manager masih tahap berikutnya.
- **UI mobile ATTB `/uimobile` (Codex, DI-COMMIT & DI-PUSH `3d572a3`, 2026-07-27):** pipeline dibuat compact dua kolom tanpa mengubah urutan `SRC→1→2→3→4→5→KI`; emoji struktural diganti Phosphor; kartu mobile hanya menampilkan pin merah + kode Gudang/Blok ringkas, sedangkan perubahan lokasi wajib lewat tombol Edit yang terlihat jelas. Import modal, target sentuh, dark mode, dan snapshot 360/390/412/768 ikut dirapikan. Build, focused ATTB 4/4, graphify, dan diff check lulus.
- **Aturan lokasi Data Stok (Codex, DI-COMMIT & DI-PUSH `15668b1`, 2026-07-27):** perubahan Blok/Lokasi oleh ADMIN langsung disinkronkan ke database tanpa approval TL, daftar Blok diurutkan natural berdasarkan kode/nama/ID, dan jalur recovery approval lama tetap tersedia. Build, graphify, dan diff check lulus.
- **Setup SQLTools ke DB self-host (Claude, 2026-07-25, DI-COMMIT).** Koneksi PostgreSQL langsung ke container `supabase-db` di `vps-dr-stack` (BUKAN lewat `supabase-pooler`/Supavisor — pooler butuh tenant identifier di username, error `ENOIDENTIFIER` kalau dipakai polos). Alur: SSH tunnel `minipc-gudang-home` → local port `15432` → IP docker-network `172.20.0.3:5432` (IP container bisa berubah kalau container di-recreate, cek ulang via `docker inspect supabase-db --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'` kalau tunnel gagal). Tunnel HARUS dibuka manual tiap sesi baru (background process, mati saat terminal/laptop restart) — tidak ada auto-start. Password `postgres` disimpan di **User Settings VS Code global** (`%APPDATA%\Code\User\settings.json`, di luar repo, `askForPassword:false`) — SENGAJA TIDAK ditaruh di `.vscode/settings.json` project (file itu ter-track & ke-push ke GitHub, taruh password production di situ = bocor ke riwayat git). Workspace `.vscode/settings.json` dikembalikan bersih (cuma `claudeCode.useTerminal`). Diverifikasi: row count `gudang`(3)/`stocks`(212) via REST API `warnoto.com` (jalur Vercel) sama persis dengan query langsung `docker exec supabase-db psql` — konfirmasi DB yang di-SQLTools = DB production yang sama dipakai Vercel.
- **Refactor split App.jsx (Claude, 2026-07-25, 9 commit lokal, DI-COMMIT — BELUM di-push):** `App.jsx` 9320→**5539 baris** (-41%). Semua JSX per-tab/modal yang aman dipisah diekstrak ke 16 file baru di `src/components/*.jsx` (murni relokasi kode — prop-drilling dari `PLNWarehouse()`, TIDAK ADA logic/tampilan yang berubah, tiap batch diverifikasi `npm run build` + sweep `eslint --rule no-undef` bersih sebelum lanjut batch berikutnya). Urutan batch (commit terpisah tiap tahap, urut kronologis dari `9abe5d1` sampai `e69674f`): (1) 18 modal besar (TUG5/9-8/10/3 form, Master Data, Akun, Stock, dll) → `TugFormModals.jsx`/`MasterDataModals.jsx`/`MasterOrgModals.jsx`/`AkunModals.jsx`/`StockModals.jsx`; (2) tab Data Stok → `DataStokTab.jsx`; (3) tab Master Data (katalog/satpam/timmutu/organisasi/gudang/akun/migrasi/auditLog/perms) → `MasterDataTab.jsx`; (4) dashboard-ringkasan + Transaction hub → `DashboardRingkasanBlock.jsx`/`TransactionHubTab.jsx`; (5) tab Penilaian Maturity (PALING HATI-HATI, redesain sesi sebelumnya diverifikasi byte-identical via diff sebelum&sesudah) → `MaturityDashboardTab.jsx`; (6) tab Approval (chip filter) → `ApprovalHubTab.jsx`; (7) sidebar navigasi → `AppSidebar.jsx`; (8) 7 modal gelombang kedua (OCR suggest, delete confirm, confirm dialog generik, photo search, lightbox, peta mini detail, capacity review) → `MiscModals.jsx`; (9) header/demo-banner/dashboard-router → `AppHeaderBar.jsx`/`DemoBannerAndToast.jsx`/`DashboardTabRouter.jsx`. **Target awal user 5000 baris — TIDAK dicapai persis, user MEMUTUSKAN CUKUP di 5539** setelah dijelaskan sisa gap (~539 baris) cuma bisa ditutup dengan memindahkan LOGIC/handler function (bukan JSX lagi) ke custom hooks — kategori pekerjaan lebih berisiko (closure atas banyak state setter) tanpa verifikasi visual browser tersedia. Lihat `CLAUDE.md` "Fakta mengikat" untuk status terbaru. **DI-PUSH ke `main`** (`94cd3d4`, 2026-07-25) — user sempat pakai `npm run dev` di localhost:3001 dan minta beberapa perbaikan visual lanjutan di menu Penilaian Maturity Gudang sebelum push (lihat commit sama di atas): lebar kiri-kanan diselaraskan ke pola `.operations-page` (dashboard + editor evidence "MATLEV AUDIT SYSTEM" sama-sama sempat masih pakai `maxWidth:1200` custom), pagination Kelola Evidence 4→5 item/halaman, gate "Kirim Hasil ke UIT" diganti dari teks merah nempel jadi pop up warning (`ConfirmDialogModal` dapat `variant:"warning"` baru), font size +1px di seluruh layar Maturity, dan kontras tab switch Dashboard/Pelaksanaan/History/5S dipertegas (fill navy solid + theme-aware pakai token `C.xxx`, sebelumnya hardcode hex yang bisa nyaris tak-terlihat di dark mode).
- **PR #3 DI-MERGE ke `main` (Claude, 2026-07-24 malam → 2026-07-25 dini hari, commit `db4d429`, deploy Vercel otomatis, `warnoto.vercel.app` terverifikasi):** `staging` (ATTB/Maturity dari Codex, lihat entri di bawah) di-fast-forward-merge ke `main` setelah user cek Preview & approve. Production sekarang termasuk ATTB compact/preview dua foto, Maturity self-host, auth/loading fixes, proxy localhost, tunnel HTTP/2, fix foto Data Stok.
- **Fix scoping UPT (Claude, 2026-07-24 malam, DI-COMMIT & DI-MERGE ke `main` bareng PR #3 di atas):** User testing staging lapor menu Alat Berat/Dashboard/ATTB menampilkan data SEMUA UPT, bukan cuma UPT login. Root cause ganda: (1) `currentUser.upt`/`uptName` tidak pernah diisi saat profil dibangun (`App.jsx` `handleAuthSession`) — semua fallback jatuh ke konstanta hardcoded `UPT="UPT Surabaya"`; (2) gate `isMSB = hasRole(currentUser,"MSB","Manager UIT")` di 4 komponen (`HeavyEquipmentTabV2.jsx`, `HeavyEquipmentDashboardSummary.jsx`, `AttbTab.jsx`, `AttbDashboardSummary.jsx`) otomatis `true` untuk SUPERADMIN (role "MSB"/"Manager UIT" tidak pernah ada di `ROLES`) — SUPERADMIN jadi otomatis lolos filter UPT. Fix: (1) resolusi `profile.upt_id`→nama UPT pendek di `userObj` (App.jsx, pakai `uptList`/`DEFAULT_UPT_LIST`); (2) fungsi kanonik `getUserUptScope()` (`src/lib/roles.js`) dipakai di ke-4 komponen alih-alih duplikat logika lokal yang cacat; (3) `isMSB` diganti jadi cek role langsung (`role==="MSB"||role==="Manager UIT"`, TANPA bypass SUPERADMIN) — SUPERADMIN sekarang JUGA terkunci ke UPT sendiri di 4 layar ini (keputusan eksplisit user, bukan lagi "lihat semua UPT" otomatis). **Ditemukan saat investigasi:** akun `fajar` ternyata masih SUPERADMIN di DB (bukan ADMIN seperti dikira user) — **role diturunkan balik ke `ADMIN`** langsung via SQL (upt_id tetap `UPT-SBY`), perlu fajar logout/login supaya cache profil ke-refresh. **Belum tuntas:** widi (TL, `upt_id=UPT-SBY` terverifikasi benar di DB) sempat lapor picker Maturity menampilkan "Gresik" bukan "Surabaya" saat testing localhost — dicurigai state HMR nyangkut (bukan bug data), user diminta hard-refresh tapi belum ada konfirmasi hasil balik.
- **Redesain menu Penilaian Maturity Gudang, sub-tab Dashboard Audit (Claude, 2026-07-25, DI-COMMIT atas instruksi user — BELUM di-push/BELUM verifikasi visual browser penuh):** rangkaian perbaikan visual iteratif atas laporan user (banner navy `.kpi-banner` sempat kehilangan padding container-level saat direfactor — dicatat sebagai gotcha di `docs/DESIGN_GUIDELINES.md` baru; halaman sempat kena `maxWidth` ganda 960/1040 lalu dihapus total lalu dikembalikan ke `1200px` supaya tidak "kejepit" tapi juga tidak meregang liar di layar lebar; blok judul "DASHBOARD AUDIT MATURITY" dan "Main Card" duplikat dihapus, isinya (WILAYAH AUDIT/status/UPT/Level Maturity) dipindah menyatu ke banner navy). **6 fitur business-logic baru** (App.jsx + `MaturityAuditSystem.jsx`): (1) tanggal "Dibuat: ..." di kartu Pelaksanaan Audit; (2) audit baru dibatasi 1x per bulan kalender per UPT (`createMaturityAudit()` cek `maturityAudits` existing bulan berjalan, block+toast kalau sudah ada); (3) pagination 5/halaman di Daftar Audit Aktif (`auditListPage`, reset saat ganti UPT); (4) tombol "Kirim Hasil ke UIT" nonaktif + hint kalau Form Pengisian 5S belum disimpan bulan berjalan (field baru `savedAt:Date.now()` di evidence `"4.5"` `k3_5s_chk`, dicek terhadap bulan sekarang); (5) History Audit cuma tampilkan `status==="FINAL"`; (6) tombol "Simpan Checklist" 5S sekarang munculkan popup konfirmasi skor (`askConfirmDelete`, pola app-wide) sebelum benar-benar simpan. **SENGAJA DITUNDA (keputusan user):** validasi wajib-upload-foto sebelum simpan checklist 5S — upload Google Drive Maturity masih dinonaktifkan total (`uploadFileToDrive()` selalu reject), block keras sekarang akan mematikan fitur Simpan Checklist sepenuhnya. `npm run build` lulus tiap tahap, semua diverifikasi arsitek baca-kode langsung (bukan cuma laporan tukang) — TAPI belum ada konfirmasi visual browser dari user untuk redesain banner+6 fitur ini.
- **`docs/DESIGN_GUIDELINES.md` baru (Claude, 2026-07-25).** Pedoman desain statis WARNOTO (gotcha `.kpi-banner` tanpa padding container-level, aturan full-width vs max-width wajar, komponen `OperationsHero`, kelas `.approval-btn--*`, pola mobile `isMobile`, tipografi/warna korporat) — dibuat setelah user MEMATIKAN pemakaian skill `ui-ux-pro-max`/agent screenshot `ui-design-reviewer` untuk task UI rutin (boros token). Rujuk file ini + baca kode langsung untuk task UI ke depan, JANGAN panggil skill/agent itu lagi kecuali user eksplisit minta.
- **Keputusan proses (Claude, 2026-07-24 malam):** user (admin utama) minta TANPA GitHub PR formal untuk kerja solonya sendiri — commit+push langsung ke `staging`/`main` (branch protection GitHub otomatis ter-bypass karena akun admin). Kontributor lain (mis. `kevinnsetiawan`, tim UI ATTB) TETAP wajib lewat PR resmi — aturan ini cuma untuk kerja arsitek/tukang atas nama user sendiri.
- **Branch `feature/import-attb-maturity` sudah selesai fungsinya (diserahkan Codex 2026-07-24, isinya sudah masuk ke `main` lewat merge di atas):** Maturity memakai tabel typed `maturity_assessments`/`maturity_audits` di self-host dan 1 asesmen lokal sudah berhasil dimigrasikan; cache browser hanya fallback baca, upload Drive tetap disabled/belum disetup. ATTB dipadatkan (pipeline, tabel, lokasi, preview korporat) dan preview material kini mempertahankan Foto Keseluruhan + Foto Nameplate, termasuk fallback item lama ke sumber TUG-10. Login default light, teks/kartu login diperbaiki, retry network 1x aktif, tunnel Cloudflare memakai HTTP/2, dan localhost memakai proxy `/supabase` untuk Auth/REST/Storage.
- **Fix terakhir Data Stok Gudang (Codex, 2026-07-24):** cache stok mempertahankan URL foto `http(s)` tetapi tetap membuang `data:`/`blob:`; seluruh jalur foto Data Stok (thumbnail, detail, edit, lightbox, pencarian, Auto-OCR) mengubah URL `https://warnoto.com/...` menjadi `/supabase/...` hanya di development agar tidak terkena `ERR_CERT_AUTHORITY_INVALID`. URL database dan production tidak berubah. Audit read-only: 212 stok, 205 punya Foto Keseluruhan, 158 punya Foto Nameplate; seluruh 158 object Nameplate ditemukan di Storage, sedangkan 47 item memang belum punya Nameplate. Build, `git diff --check`, `graphify update .`, focused Playwright ATTB dan Data Stok masing-masing lulus.
- **Paket responsive Codex 2026-07-20 siap dipublikasikan:** audit Playwright mencakup 360×800, 390×844, 412×915, 768×1024, serta desktop 1366px. Perbaikan meliputi shell/header/drawer, Dashboard Eksekutif, Data Stok, Kapasitas Gudang, Master Katalog, Struktur Organisasi, Master Gudang, Migrasi, Audit Log, Matrix Izin, Alat Berat/Peminjaman, form/modal, dan `mobile-preview.html`. Build, targeted responsive 32/32, desktop smoke 2/2, `git diff --check`, serta localhost HTTP 200 lulus. **Namun user menyatakan belum puas dengan kualitas visual UI/UX mobile; acceptance visual masih terbuka dan hasil ini harus dianggap baseline teknis, bukan desain final.**
- **Operasional lokal (2026-07-19):** dev server WARNOTO aktif di `http://localhost:3001` dengan Vite binding ke `localhost`/IPv6 `::1`; verifikasi langsung menghasilkan HTTP 200.
- **Perbaikan mobile P0/P1 selesai, belum commit (2026-07-19):** header 360px dirapikan menjadi satu baris dengan target sentuh 44px; grid Dashboard Analitik, Kapasitas/Peta Gudang, Alat Berat, Material Cadang, Rencana Kedatangan, Approval, TUG-3/TUG-15, dan Telegram menjadi satu kolom pada `<=768px`; filter Peta/Kapasitas full-width; modal target memakai `90dvh`; demo banner aman terhadap viewport/safe-area; tabel tetap scroll lokal. Tidak ada perubahan logika bisnis/schema/API/dependency. `npm run build`, `git diff --check`, dan smoke test `localhost:3001` lulus. Masih perlu validasi visual user pada 360×800 dan 390×844.
- **Preview mobile desktop selesai, belum commit (2026-07-19):** `mobile-preview.html` menyediakan frame WARNOTO dengan viewport internal asli 360×800 atau 390×844, scale-to-fit layar desktop, dan tombol muat ulang. Diakses melalui `http://localhost:3001/mobile-preview.html`; HTTP 200 dan `git diff --check` lulus. Halaman ini khusus dev server lokal dan tidak mengubah aplikasi utama.
- **Perbaikan targeted Alat Berat & Peminjaman selesai, belum commit (2026-07-19):** `OperationsHero` kini merender eyebrow/judul/deskripsi yang sebelumnya diabaikan; banner mobile memakai KPI 2 kolom pada 360/390, Fleet Registry memakai rail filter horizontal + kondisi full-width, kartu alat satu kolom tanpa overflow, serta form/list/approval peminjaman ditata satu kolom dengan target sentuh 44px. File: `OperationsHero.jsx`, `HeavyEquipmentTabV2.jsx`, `src/styles/operations.css`. Tidak ada perubahan logika bisnis/API/schema/dependency. Build/diff check dan preview HTTP 200 lulus.
- **Selesai & dipublikasikan ke `main` (shift-shift sebelumnya):** Pak War humanis + fix 401 Groq (`efb9fca`), floor font 12px + toggle sidebar footer (`6a09f83`), paket Codex `df92373` (foto satpam, mode Alat Berat, banner KPI navy compact di 6 permukaan, UX Kapasitas/Forecast/TUG), paket UI Pak War `07584e6` (fix overlap welcome mobile, FAQ/Telegram halaman penuh + tombol kembali, progres sinkron 0–100% via `onProgress` di `syncRagChunks`).
- **Selesai & di-commit (shift Claude 2026-07-15 sore):** paket TUG-10 + pagination Forecast — (a) cascade lokasi Gudang→Sub Gudang→Blok di form TUG-10 (kontrak txn tetap `lokasiTujuanId`, plus `gudangTujuanId`/`subGudangTujuanId`/`satpamId`; legacy blok via sentinel `__legacy__`), (b) satpam punya `gudangId` di jsonb (tanpa ubah skema), Master Data satpam dikelompokkan per gudang, select satpam TUG-10 terfilter gudang (auto-pilih bila 1), tanda tangan "SATPAM GUDANG <nama>" di `buildTUG10HTML` — signature fungsi kini `(txn, katalogList, lokasiList, users, satpamList, gudangList, subGudangList)`, `downloadTUG10HTML` ikut berubah (showToast pindah ke argumen terakhir), (c) fix "tombol Ajukan mati": state `savingTxn` + checklist kelengkapan live via `tug10Missing()` + scroll-highlight `flagTug10Invalid()` + banner role non-ADMIN/TL; catatan: validasi kini menuntut SEMUA baris barang lengkap (baris kosong harus dihapus, tidak lagi difilter diam-diam), (d) kartu material "Barang #N" collapsible + upload foto kompak, (e) pagination Forecast Stok: 20/50/100 per halaman (default 20) + Sebelumnya/Berikutnya + reset ke hal.1 saat filter/search/sort berubah (`ForecastStokPage.jsx`, class `.forecast-pagination` di `src/index.css`). Build LULUS. Masih perlu dicek user: layout cetak 4 kolom tanda tangan TUG-10; verifikasi end-to-end foto satpam (SP001/SP002 belum ada foto).
- **Selesai, di-commit & di-push ke `main` (deploy Vercel otomatis):** fix "Ajukan TUG tidak terjadi apa-apa" — (i) overlay global "Menyimpan Transaksi" (spinner navy + progres upload foto x/y, state `savingInfo`, App.jsx ~4166, kelas `.txn-spinner`), `catch` di `commitNewTxn` (error simpan kini toast merah, tidak diam), timeout 30 detik per upload foto di `processTxnPhotos` (param opsional `onProgress`); (ii) akar masalah: `_isDataUrl` dipakai `commitNewTxn` tapi tak diimpor (sisa refactor split App.jsx) — mematikan Ajukan di SEMUA jenis TUG secara diam-diam. Sweep eslint no-undef menemukan total 6 import/prop hilang, semua diperbaiki: `_isDataUrl` (export supabaseSync + import App.jsx), 2 helper alat berat di `HeavyEquipmentDashboardSummary.jsx`, 2 helper alat berat di `docBuilders.js` (`buildHeavyEquipmentLoanHTML`), `compressImage` di `rag.js` (`ocrSpaceOCR`), prop `isMobile` di `RencanaKedatanganTab.jsx` + call site. Build LULUS, sweep no-undef 0 error. Tips sweep ulang: `npx --yes eslint@8 --no-eslintrc --parser-options=ecmaVersion:2023,sourceType:module,ecmaFeatures:{jsx:true} --env browser,es2023 --rule '{"no-undef":"error"}' App.jsx src/lib/*.js src/components/*.jsx`.
- **Selesai, di-commit & di-push ke `main`:** fix "Memuat sesi..." selalu lama saat buka web — akar masalah: startup menunggu 2 roundtrip berurutan (refresh token Auth + fetch `profiles` di dalam callback `onAuthStateChange`) sebelum `authLoading` false. Solusi cache-first (App.jsx saja): profil user di-cache di localStorage (`warnoto_profile_cache_v1`, helper `readCachedProfile` module scope), `currentUser`/`authLoading` init dari cache → app langsung tampil tanpa network; profil di-refresh di latar belakang, cache dibuang saat logout/sesi invalid (auto-lempar ke login); callback `onAuthStateChange` kini non-async (hindari deadlock lock auth per docs supabase-js); semua akses localStorage diguard try/catch. Role client tetap kosmetik — penjaga asli RLS. Build LULUS.
- **Selesai, di-commit & di-push ke `main` (2026-07-16 sore):**
  1. **MODE DEMO per-tab** (`src/lib/demo.js` baru; flag sessionStorage `warnoto_demo`) — trial TUG tanpa menyentuh database. Toggle di menu akun (`🧪 Mode Demo (TUG)` di atas Logout) + banner pill oranye `.demo-banner` (fixed bottom) dengan tombol Keluar. Di tab demo SEMUA penyimpanan keluar dibekukan via guard `isDemoMode()` di choke point: `CLOUD.set` (cloud.js); `syncMasterTable`/`syncMaterialCadangRows` (masterSync.js); `processTxnPhotos` (return referensi txn yang SAMA supaya tidak memicu save-loop `_fotoPending`), `syncTUG15ToSupabase`, `syncStockQtyToSupabase`, `syncFotoMaterialToSupabase` (supabaseSync.js); `syncRagChunks`/`syncWarnotoState`/`syncStocksSnapshot`/`uploadMaraToDB`/`submitAkunBaru`/`submitAkunEdit`/`submitGantiPassword` (App.jsx); handler tulis `AIFaqPanel` & `TelegramWhitelistPanel`. Reads tetap live; tab/device lain tidak terpengaruh (pengisian lokasi barang operator lain aman).
  2. **Restyle tombol Approval + form TUG-10** (ApprovalTab.jsx, App.jsx:7662, kelas `.approval-actions`/`.approval-btn--*`/`.tug-add-item` di index.css) — 24 tombol seragam korporat-modern (Setujui hijau gradasi + ikon ✓, Tolak outline merah, Konfirmasi Tolak merah solid, Batal netral, Lengkapi TUG-7 navy); auto-width rata kanan di desktop, full-width 44px di mobile, varian `--compact` untuk baris Lokasi/Blok; `+ Tambah Barang Retur Lain` jadi dashed + hover navy. Logika onClick tidak berubah.
- **Selesai & DI-PUSH ke `main` (deploy Vercel otomatis):** (a) `e6b769f` seragamkan 55 tombol Setuju/Tolak approval seluruh app ke `.approval-btn--*` (termasuk temuan TUG5Tab/TUG3Tab yang terpisah dari ApprovalTab) + `setupmobile.md` (perintah kerja responsive); (b) **AUDIT LOG lengkap** — tabel Supabase `audit_log` (migrasi `create_audit_log`, append-only, RLS insert/select authenticated) + helper `src/lib/audit.js` (`logAudit()` fire-and-forget, skip Mode Demo) + ~50 titik instrumentasi (LOGIN hanya SIGNED_IN; approval non-TUG via satu pintu `logApprovalHistory`; approval TUG 19 titik langsung di fungsi keputusan TUG-3/4/5/7/8/9/10; CREATE txns; CRUD master; IMPORT MARA/ATTB/kapasitas/migrasi; akun) + halaman `AuditLogPage.jsx` ADMIN-only (sub-menu Master Data, filter aksi/user/tanggal, pagination server-side 50). Build LULUS.
- **Selesai & DI-PUSH ke `main`:** (c) **RBAC** (`e6f5f6e`) — Matrix Izin per role (src/lib/perms.js `can()`, default = replika gating existing; override tabel `role_permissions`, RLS tulis khusus ADMIN/SUPERADMIN; halaman PermMatrixPage sub-menu Master Data; approval chain tetap hardcoded) + batasan per gudang (`profiles.gudang_ids` null=semua; checkbox di Kelola Akun; filter `visibleGudangList` di TUG-10/Master/Data Stok/Kapasitas/Peta; edge function admin-create-user v7 & admin-update-user v5 SUDAH DI-DEPLOY); (d) **Import Excel baru** — Import Master Lokasi (Master Gudang: template+modal review-first `ImportLokasiModal.jsx`, persist via syncLokasi existing) + Import Transaksi TUG Lama (Migrasi Data: histori murni APPROVED `legacyImport:true`, TIDAK memutasi stok/docSeq, dedup No Dokumen, badge Legacy di daftar TUG generik). Build LULUS, sweep no-undef 0.
- **Program fitur (keputusan user 2026-07-16, task list): SEMUA SELESAI & DI-PUSH.** 1) audit log ✅ → 2) RBAC ✅ → 3) import Excel ✅ → 4) responsive `setupmobile.md` ✅ (`70836cc`) → 5) dark mode ✅ (`81a50b9`, user sudah verifikasi visual). i18n DITUNDA (belum digarap). Mode Demo per-tab tersedia untuk trial user.
- **Fix akses RBAC (2026-07-17):** akun `widi` (role TL) tidak melihat menu Matrix Izin/Audit Log/Migrasi Data — gating `hasRole(currentUser,"ADMIN")` di App.jsx:4440 memang sengaja admin-only, BUKAN bug. Keputusan user: bukan ubah role widi, tapi role `fajar` (Fajar Sutomo) diubah dari ADMIN → **SUPERADMIN** langsung via SQL (`update profiles set role='SUPERADMIN' where username='fajar'`, sudah dieksekusi & di-verify). Setelah itu ketahuan bug sidebar terpisah (lihat "Keputusan arsitektur" di atas) — sudah diperbaiki & di-push bareng dark mode (`81a50b9`). Fajar masih perlu logout/login (atau clear `warnoto_profile_cache_v1`) supaya cache profil ke-refresh.
- **Responsive `setupmobile.md` — Stage 1 (tabel) SELESAI, BELUM commit:** audit 26 tabel React di app. 24 sudah punya wrapper `overflowX:auto` (OK, tak disentuh). 2 diperbaiki (dibungkus `overflowX:auto`/`WebkitOverflowScrolling:touch` + `minWidth:640` mengikuti pola `.forecast-table-card`): `App.jsx:5724` (tabel Kelola Akun) dan `src/components/MigrasiDataTab.jsx:789` (tabel review qty di luar Unrestricted Stock). Build LULUS, `git diff --check` bersih, hanya 2 file di working tree. BELUM diverifikasi visual (environment tanpa browser automation) — user perlu cek manual di browser (resize/devtools) sebelum commit, terutama viewport 360×800 & 768×1024.
- **Responsive `setupmobile.md` — Stage 2 (modal & form) SELESAI, BELUM commit (2026-07-17):** inventory 43 modal/overlay (29 inline App.jsx + 14 di src/components). 13 diperbaiki: overlay tanpa `padding` ditambah `padding:16`; 8 modal card yang sebelumnya tanpa `maxHeight`/`overflowY` (lokasi, satpam, timMutu, gudang-edit, maturity, uit, upt, ultg, akun, gantiPassword, MaterialCadang applyConfirm) ditambah `maxHeight:"90dvh",overflowY:"auto"`; semua `90vh/92vh/85vh` diganti `dvh` (supaya keyboard virtual HP tidak menutupi tombol Simpan); 3 grid 2-kolom kaku (stockModal, katalogModal, ocrSuggest) jadi `isMobile?"1fr":"1fr 1fr"`. Sisanya (30 modal) sudah OK, di-skip. File: `App.jsx`, `src/components/MaterialCadangTab.jsx:818`. Build LULUS (diverifikasi ulang arsitek), diff dicek manual — konsisten dengan laporan tukang, tidak ada perubahan logika. Catatan sisa: modal di `src/components/*` (ApprovalTab, AttbTab, TUG3/5Tab, HeavyEquipmentTabV2, dll) masih pakai `90vh/92vh` bukan `dvh` — sudah fungsional (maxWidth:100%+overflowY:auto), sengaja tidak disamakan supaya diff tetap minimal; kalau mau full-konsisten `dvh`, itu keputusan terpisah nanti.
- **Responsive `setupmobile.md` — Stage 1+2+3 DI-COMMIT & DI-PUSH ke `main`** (`70836cc`, 2026-07-17, deploy Vercel otomatis): user konfirmasi lanjut tanpa menunggu laporan verifikasi visual detail per viewport — kalau nanti ditemukan ada yang berantakan di viewport tertentu, tangani sebagai perbaikan bertarget, bukan revert.
- **Responsive `setupmobile.md` — Stage 3 (Pak War/KPI/dashboard/sweep akhir) SELESAI (2026-07-17):** Pak War (`AIAgentPage.jsx`) & banner KPI (`.kpi-banner` + 4 pemakainya + `OperationsHero`) sudah class-driven dengan breakpoint lengkap dari shift sebelumnya — tidak ada yang perlu diperbaiki. Dashboard: `DashboardManager.jsx` (header eksekutif `flex-direction` kolom di HP + grid widget `2fr 1fr`→`1fr`) dan `DashboardAsman.jsx` (grid widget sama, sibling pola identik) diperbaiki via prop `isMobile` baru (diteruskan dari `App.jsx:4600,4620`); `DashboardDefault.jsx`/`DashboardMaturityBanner` sudah class-driven, tak disentuh. Sidebar diverifikasi ulang: masih sesuai spec 260/76px + drawer ≤768px, TIDAK diubah (keputusan final). Sweep grep lintas `src/`+App.jsx untuk fixed-width/absolute-position/nowrap-anti-pattern: semua false-positive (ikon kecil, elemen desktop-only, sudah ellipsis/wrap aman) — tidak ada perbaikan tambahan. Build LULUS (diverifikasi ulang arsitek — App.jsx cuma +2 baris `isMobile={isMobile}`, dicek diff manual). **INI STAGE TERAKHIR** dari roadmap responsive — 3 stage semuanya selesai, semua masih di working tree (belum commit), menunggu verifikasi visual user.
- **PWA: manifest + ikon home-screen (2026-07-17, DI-COMMIT & DI-PUSH `96bffab`).** Bug: logo tidak muncul saat WARNOTO di-install ke home screen HP. Root cause: `index.html` tak punya `<link rel="manifest">`/`apple-touch-icon`; satu-satunya aset (`public/favicon.png`) potret 2480x3397 non-persegi (emblem+wordmark "PLN"+banyak whitespace) tak layak dipakai ikon. Fix: crop emblem kotak kuning+petir dari `favicon.png` (bbox deteksi warna kuning, dipad jadi persegi 2017x2017) → `public/icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png` (RGB solid, tanpa alpha, khusus iOS); `public/manifest.webmanifest` baru (`background_color`/`theme_color` dari `src/theme.js` `C.bg`/`C.sidebar`); `index.html` dapat 5 tag baru (manifest, apple-touch-icon, theme-color, apple-mobile-web-app-capable/title) — baris `<link rel="icon">` favicon lama TIDAK disentuh (di luar scope laporan ini). Tidak ada dependency baru/service worker. Efek baru terlihat setelah user install ulang/refresh PWA di HP.
- **Perbaikan targeted mobile: Forecast Stok, Stock Opname, ATTB — DI-COMMIT & DI-PUSH ke `main` (`a067196`, 2026-07-20, deploy Vercel otomatis).** Respon langsung atas keluhan visual spesifik user (bukan redesign global): (1) Forecast Stok — list material jadi vertical card summary (`mobile-card-table`, title+status kelihatan, sisanya via `data-label`, klik kartu tetap buka detail penuh), border accent warna risiko lewat CSS var `--risk-accent` (fix: inline `borderLeft` sebelumnya kalah oleh `!important` base rule); tombol filter status kritis/dll tidak lagi kepencet (hapus `flex:1` yang kontradiktif di breakpoint 480px); banner KPI dirapikan compact-corporate di mobile (padding/font mengikuti pola `.capacity-summary-banner`). (2) Stock Opname — tombol Opname SAP/Non-SAP + bar submit + pagination disamakan ke `.approval-actions`/`.approval-btn--*` (komponen yang sudah terbukti simetris & responsive di ApprovalTab/AttbTab), ganti dari `sty.btn` ad-hoc yang tak punya `minWidth`/`flex` (akar penyebab tombol asimetris/melebar di seluruh app). (3) ATTB — tabel pool Material Bongkaran + tabel utama dikonversi ke `mobile-card-table` vertical view, TANPA menyembunyikan kolom (beda pendekatan dari Forecast — ATTB tak punya halaman detail terpisah, semua field termasuk edit inline Lokasi tetap fungsi inti). Tidak ada perubahan logika bisnis/schema/API. Build lulus. **Belum ada konfirmasi visual user di browser** (environment tanpa browser automation) — verifikasi manual di `http://localhost:3001/mobile-preview.html` masih perlu dilakukan.
- **Tambahan skill `ui-ux-pro-max-cli` — DI-COMMIT & DI-PUSH (`3599a0c`, 2026-07-20).** Atas permintaan user: `npm install -g ui-ux-pro-max-cli` + `uipro init --ai claude` (paket registry diverifikasi legit sebelum install). Menambah 7 skill baru di `.claude/skills/` (ui-ux-pro-max, design, design-system, ui-styling, brand, banner-design, slides), tidak menyentuh kode/config lain. Perlu restart Claude Code supaya skill ter-load.
- **Fix bug pipeline ATTB mobile — DI-COMMIT & DI-PUSH `c12ef76` (2026-07-20).** User laporan `a067196` masih "sangat berantakan" di mobile, khusus kartu tahap pipeline + chip filter. Investigasi via DOM measurement (screenshot/resize_window browser automation rusak total di environment ini — timeout `Page.captureScreenshot`, `innerWidth` tak berubah; workaround pakai iframe asli `mobile-preview.html` yang punya viewport internal sungguhan 390px, ukur `getBoundingClientRect()` langsung). Root cause: `.attb-stage-card` (src/styles/operations.css) tidak punya `flex-shrink:0` di dalam `.attb-pipeline` (`display:flex; overflow-x:auto`) — `min-width:94px !important` tetap dilanggar, kartu menyusut sampai 44px lebar/117px tinggi dengan teks wrap parah. Fix: `flex:0 0 auto !important` pada `.attb-stage-card` + `flex-shrink:0` pada `.attb-pipeline > div:not(.attb-stage-card)` (pemisah panah). Diverifikasi 2x independen (subagent + pengukuran ulang arsitek): kartu sekarang 92–214px lebar × 78px tinggi seragam, container tetap scroll horizontal (scrollWidth 1121 > clientWidth 347) sesuai desain. Chip filter (`.operations-segments`) sendiri tidak ada bug terpisah — kemungkinan besar keluhan "chip berantakan" adalah efek visual dari pipeline yang rusak di sebelahnya, bukan bug independen; belum ada konfirmasi eksplisit dari user karena screenshot tak tersedia. Build lulus. **User belum memberi sign-off visual atas fix ini** — masih perlu dicek langsung di `mobile-preview.html`.
- **Fix "update lokasi Data Stok hilang setelah refresh di Vercel" — DI-COMMIT & DI-PUSH ke `main` (`bfb469d`+`515bd2c`, 2026-07-21, deploy Vercel otomatis).** User laporan: pindah blok lewat dropdown tabel Data Stok (gudang sama, harusnya langsung tersimpan tanpa approval) menampilkan toast "berhasil", tapi hilang lagi setelah refresh. Diagnosa (Supabase Management API, read-only, token `SUPABASE_ACCESS_TOKEN` dari `.env`): grants & RLS policy tabel-tabel master sudah benar (dicek eksplisit untuk `warehouse_capacity`/`warehouse_capacity_imports`, dan RLS-enabled dikonfirmasi di semua 32 tabel public) — bukan masalah skema/permission server. Root cause nyata ada 2, dua-duanya di `src/lib/masterSync.js`/`App.jsx` (fungsi generik dipakai semua tabel master, termasuk `stocks`): (1) `syncMasterTable()` upsert tanpa dedupe — Postgres `ON CONFLICT DO UPDATE` GAGAL TOTAL (error 21000) kalau ada `id` yang sama muncul >1 kali dalam satu batch; (2) `saveToCloud()` (App.jsx) tidak pernah mengecek hasil `Promise.all(syncPromises)` — kalau sync ke Supabase gagal (sebab apa pun), UI tetap menampilkan toast "berhasil" karena caller tak pernah tahu ada kegagalan; user baru sadar saat refresh me-load ulang dari Supabase (sumber kebenaran) dan datanya tidak pernah benar-benar tersimpan. Fix: (1) dedupe-by-id keep-last sebelum upsert di `syncMasterTable`, plus `error.message` eksplisit di semua `console.error` sinkron di file itu; (2) `saveToCloud` sekarang melacak label manusiawi per tabel (`syncTasks` alih-alih array promise polos), cek hasil tiap task, dan tampilkan toast merah `⚠️ Sebagian data gagal tersimpan ke cloud (<label>). Coba simpan ulang atau cek koneksi.` kalau ada yang gagal — berlaku untuk SEMUA tabel yang lewat `saveToCloud` (Data Stok, Master Katalog, Kapasitas Gudang, Alat Berat, ATTB, Stock Opname/Count), bukan cuma Data Stok. Kedua fix dikerjakan tukang-sonnet (2 delegasi terpisah), direview arsitek (diff dibaca penuh), `npm run build` lulus tiap kali. Deploy: staging di-push dulu, lalu `main` di-fast-forward-merge ke staging (`git merge --ff-only`) dan di-push — Vercel auto-deploy dari `main`.
- **INSIDEN DATA PRODUCTION: lokasi & foto SELURUH Data Stok hilang — root cause ditemukan & DIPERBAIKI, DI-COMMIT & DI-PUSH ke `main` (`a615dfa`, 2026-07-21, deploy Vercel otomatis).** Beberapa jam setelah fix di atas, user lapor lokasi DAN foto hilang total (bukan cuma 1 item). Investigasi via Supabase Management API (SQL langsung ke DB production, read-only) mengonfirmasi **ini kehilangan data nyata di server**, bukan bug tampilan: 212/212 baris `stocks` kehilangan referensi foto, 209/212 kehilangan `lokasi_id` — termasuk 68 baris **non-SAP** yang mustahil tersimpan tanpa lokasi+foto (divalidasi wajib di `saveStock()`), jadi datanya pasti TERTIMPA, bukan memang belum pernah diisi. **Root cause** (BUKAN disebabkan 2 fix sebelumnya — bug lama, baru ketahuan sekarang): fungsi `loadCloud()` (App.jsx) punya 9 titik identik yang memakai `if (remote && remote.length>0)` untuk memutuskan pakai-data-server vs dorong-cache-lokal-ke-server — kondisi ini TIDAK BISA membedakan `loadMasterTable()` **gagal** (return `null`, mis. saat Supabase project pause/resume atau network blip — log Postgres production mengonfirmasi ada restart DB persis di jam kejadian) dari tabel yang **memang genuinely kosong**. Saat fetch gagal, cache localStorage browser yang basi (foto/lokasi sudah lama tak sinkron) ikut terdorong ke server via `syncMasterTable`, menimpa data benar lewat mekanisme upsert+reconciliation-delete (yang mengganti SELURUH kolom `data` jsonb per baris). Pola yang salah ada di: katalog, stocks, stock_opname, stock_count, heavy_equipment, heavy_equipment_loans, attb_list, warehouse_capacity, warehouse_capacity_imports. Tabel yang pakai `seedMasterTableIfEmpty` (uit/upt/gudang/lokasi/satpam/tim_mutu) SUDAH AMAN dari awal (helper itu sudah benar: `if (existing===null) return defaults` tanpa push). **Fix**: 9 titik itu dipecah jadi 3 cabang eksplisit (null=gagal-fetch → tampilkan lokal saja, JANGAN push; `[]` genuinely kosong → tetap aman dorong lokal seperti sebelumnya untuk migrasi instalasi baru; ada isi → pakai data server) + toast peringatan kalau ada tabel gagal dimuat (`⚠️ Gagal memuat sebagian data dari cloud (...). Menampilkan data lokal sementara — JANGAN edit sampai refresh berhasil`). Dikerjakan tukang-opus (bug lintas 9 lokasi, butuh presisi), direview arsitek penuh (semua 9 diff dibaca), `npm run build` lulus. **Pemulihan data — FOTO (tahap 1)**: BERHASIL — file foto asli masih fisik ada di Supabase Storage (bucket `stock-photos`, terverifikasi 356 file utuh) + tabel `stock_photo_embeddings` masih punya pemetaan katalog→URL; arsitek jalankan 1 UPDATE SQL fill-if-empty (replikasi logika `scripts/migrate_material_photos.mjs` baris 401-402: `utama→fotoKeseluruhan`, `tambahan→fotoNameplate`) → 198/212 dapat `fotoKeseluruhan`, 158/212 dapat `fotoNameplate`.

**Pemulihan data — LOKASI (tahap 2, SUKSES BESAR)**: user ternyata punya DR backup mandiri yang SEBELUMNYA TIDAK DIKETAHUI arsitek — cron `pg_dump` per jam ke mini PC lokal (`minipc-gudang`, lihat "Fakta mengikat" di atas untuk detail akses). Arsitek SSH masuk (key sudah ada dari sesi lampau, tinggal pakai `Host minipc-gudang` di `~/.ssh/config`), temukan dump `warnoto-backup-20260721-0700.dump` (21.9MB, ukuran NORMAL) vs dump jam 08:00 (cuma 4.2MB, JAUH lebih kecil — ini yang mengonfirmasi insiden terjadi tepat di jendela 07:00-08:00 UTC). Restore dump 07:00 ke container Postgres sekali-pakai di mini PC itu sendiri (bukan ke production), ekstrak `stocks.lokasi_id` yang masih terisi (161/212 baris), validasi silang ke production (semua id cocok, semua `lokasi_id` masih valid di tabel `lokasi` saat ini, 3 baris sudah keburu terisi manual — dilewati), lalu UPDATE fill-if-empty ke production via Management API (`lokasi_id` kolom + `data.lokasiId` jsonb, dua-duanya harus konsisten). **Hasil akhir: lokasi 161/212 (dari 3), foto 198/212 — 51 baris sisanya memang sudah kosong SEBELUM insiden (tidak ada di backup 07:00 juga), bukan bagian dari kerugian.** User sudah konfirmasi foto muncul. **Regresi diverifikasi (2026-07-21 lanjutan 3):** user coba ulang edit-lokasi baru langsung di production (ubah lokasi 1 barang → refresh) dan konfirmasi "sudah aman" — fix `a615dfa`+`515bd2c`+`bfb469d` terbukti menahan bug ini, tidak berulang.

**Tindak lanjut disarankan**: pertimbangkan mengaktifkan retensi lebih panjang / redundansi kedua untuk `vps-backup` di mini PC (saat ini 7 hari, satu lokasi) — backup inilah yang menyelamatkan 161 baris lokasi hari ini; kalau mini PC itu sendiri bermasalah, tidak ada cadangan lain sama sekali (Supabase PITR masih mati). **[SUDAH DITINDAKLANJUTI 2026-07-21 lanjutan — lihat poin 1 Keputusan Arsitektur: retensi naik ke 730 hari, plus pipeline kedua khusus auth/storage/vault.]**

- **`graphify` peta baca codebase — sebagian DI-COMMIT & DI-PUSH ke `main` (`470eea0`, 2026-07-22).** `CLAUDE.md` (aturan pakai) + `.gitignore` (`graphify-out/`) sudah live. **BELUM ke-commit:** `.claude/settings.json` (hook `PreToolUse`) — diblokir classifier auto-mode untuk Claude, masih di working tree lokal, perlu user commit manual (lihat "Langkah berikutnya"). Graph sudah tervalidasi jalan (`graphify god-nodes` menampilkan `PLNWarehouse()` sebagai hub, sesuai ekspektasi); `GRAPH_REPORT.md` sudah di-generate (`--no-label`, tanpa LLM).
- **Persist tab aktif — DI-COMMIT & DI-PUSH ke `main` (`c1e4fec`, 2026-07-22, deploy Vercel otomatis).** Tab yang lagi dibuka disimpan ke `sessionStorage` (`warnoto_tab`) supaya refresh halaman tetap di menu yang sama; dibersihkan saat logout. Tidak ada perubahan logika bisnis lain. Build lulus.
- **Dashboard overview: 3 fix UX kecil + pilot icon Phosphor — DI-COMMIT & DI-PUSH ke `main` (`fbd7898`, 2026-07-22, deploy Vercel otomatis).** Dark-mode fix kotak icon KPI (tadinya terang mengambang di kartu gelap), tap target tombol close popup KPI jadi 44×44, `tabular-nums` di angka KPI/Rp/qty; icon emoji→Phosphor di 3 varian dashboard (Default/Manager/Asman). Detail scope & pengecualian ATTB di "Keputusan arsitektur". Build lulus tiap tahap, diverifikasi arsitek independen (bukan cuma laporan tukang).
- **Fix bug sync Kapasitas Gudang ke Supabase — DI-COMMIT & DI-PUSH ke `main` (`65fedbe`, 2026-07-22, deploy Vercel otomatis, termasuk 1 migration Supabase).** Root cause & detail lengkap di "Keputusan arsitektur". `npm run build` lulus untuk kedua tabel, diff direview arsitek independen, migration diverifikasi lewat `information_schema` sebelum lanjut ke kode.
- **Koordinasi ATTB UI/UX.** Area ATTB tetap memiliki ownership gate karena pernah diserahkan ke tim user. Pada 2026-07-27 user mengonfirmasi tidak ada overlap lalu menyetujui Codex memperbaiki UI mobile secara terbatas; hasilnya ada di commit `3d572a3`. Perubahan ATTB berikutnya tetap harus mengecek overlap terlebih dahulu.
- **Fix sync stocks/katalog full-table-resync — DI-COMMIT & DI-PUSH ke `main` (`5dff883`, 2026-07-22, deploy Vercel otomatis).** User lapor update lokasi Data Stok tersimpan tapi lambat (15-20 detik). Root cause: `saveToCloud()` (App.jsx) selalu mengirim SELURUH array `stocks`/`katalogList` ke `syncMasterTable()` walau cuma 1 baris berubah — untuk `stocks` (212 baris, 5 di antaranya punya foto base64 di `data.fotoKeseluruhan`) ini berarti payload ~18.7MB per edit kecil. Fix: fungsi baru `syncMasterTableRows(table, rows, extraCols)` di `src/lib/masterSync.js` (upsert-only, TANPA reconciliation-delete) + parameter kedua opsional `hints` di `saveToCloud(overrides, hints)` — kalau caller bisa enumerasi baris yang berubah (mis. `{stocksChangedRows:[updated]}`), pakai sync ringan; kalau tidak, fallback ke `syncMasterTable` penuh seperti semula (dipertahankan SENGAJA untuk 4 titik yang butuh reconciliation-delete: `deleteKatalog`, `deleteStock`, `approveStockDelete`, `approveOpname_Manager`). 17 titik panggilan dioptimalkan (termasuk 2 handler pindah-lokasi Data Stok yang jadi biang keladi laporan user). Dikerjakan tukang-opus, direview arsitek penuh (semua 17+4 titik dibaca satu-satu via grep+read, bukan cuma percaya laporan), `npm run build` dijalankan ulang oleh arsitek — lulus. Database production (minipc-gudang) juga dicek langsung via SQL: 0 baris `stocks` yang orphan/`katalogId` tak match `katalog` — data konsisten.
- **Fix cache-first "Memuat data dari cloud..." — DI-COMMIT & DI-PUSH ke `main` (`a3f2b89`, 2026-07-23, deploy Vercel otomatis).** Lanjutan dari pending item kemarin (17 request paralel bikin layar blocking lambat tiap refresh). Rencana lengkap: plan mode dulu (2 subagent Explore — hasil: `stocks`/`katalog`/`lokasi`/`gudang`/`attbList`/`heavyEquipment(+Loans)` dipakai LINTAS TAB termasuk 2 modal global, jadi **lazy-load per tab ditolak** karena terlalu invasif/berisiko; dipilih **cache-first** sesuai pola "Memuat sesi..." yang sudah terbukti jalan sebelumnya), plan disetujui user, dieksekusi tukang-opus, direview arsitek penuh (diff dibaca, `npm run build` dijalankan ulang independen). Implementasi: `readCachedList()` (App.jsx, dekat `readCachedProfile`) + lazy-initializer `useState` untuk 11 tabel yang sudah punya cache localStorage (`stocks`/`katalogList`/`heavyEquipmentList(+Loans)`/`attbList`/`opnameList`/`stockCountList`/`gudangCapacityList(+Imports)`/`txns`/`docSeq`) — app render LANGSUNG dari cache, `loadCloud()` (17 query, logika null/empty/has-data 3-arah) TIDAK diubah sama sekali, cuma tidak lagi memblokir render pertama; layar blocking cuma muncul kalau device/browser benar-benar baru (belum ada cache stocks MAUPUN katalog). Indikator "Menyinkronkan data..." ditambahkan di widget cloud-status sidebar yang sudah ada (bukan komponen baru). **Bug tersembunyi ikut diperbaiki**: `CLOUD.set()` (`src/lib/cloud.js`) menelan `QuotaExceededError` diam-diam — `stocks` bisa ~18.7MB (foto base64 di 5/212 baris) jauh di atas kuota localStorage, kemungkinan besar cache-nya gagal tersimpan diam-diam selama ini; sekarang di-strip jadi versi "lean" (`leanStocks()`, buang `fotoKeseluruhan`/`fotoNameplate`) SEBELUM di-cache — state React & data di server tetap lengkap dengan foto, cuma salinan cache yang tanpa foto. Cache dibersihkan saat logout (cegah bocor ke user lain di device sama). **Terverifikasi user langsung di `localhost:3001` sebelum commit**: layar blocking sudah hilang di refresh biasa, localStorage total 4.02MB (jauh di bawah kuota ~10MB Chrome), edit data tetap tersimpan normal. **Catatan sisa (bukan bug dari fix ini)**: toast "⚠️ Gagal memuat sebagian data dari cloud..." (mekanisme LAMA, sudah ada sebelum hari ini) kadang muncul saat refresh — salah satu dari 17 request background sesekali gagal/timeout ke server self-host (kemungkinan Cloudflare Tunnel/beban server), fallback ke cache lama sudah benar jalan. **Root cause "gagal memuat"/"foto hilang" DITEMUKAN & DIPERBAIKI sesi yang sama** — lihat entri terpisah di bawah.

- **Fase 2 cache-first (8 tabel master data) — DI-COMMIT & DI-PUSH ke `main` (`8101c3a`, 2026-07-23, deploy Vercel otomatis).** Perluasan Fase 1 ke `lokasi`/`gudang`/`sub_gudang`/`satpam`/`tim_mutu`/`uit`/`upt`/`ultg` — tabel yang SEBELUMNYA tidak pernah ditulis ke localStorage sama sekali (CRUD-nya langsung ke Supabase via `syncMasterTable`/wrapper, bypass `saveToCloud`). Pola SAMA PERSIS Fase 1: `PHASE2_CACHE_KEYS` (kunci baru untuk 7 tabel + `pln_lokasi_v4` yang ternyata SUDAH ADA sebagai kunci baca-saja tak terpakai, direuse), 8 lazy-initializer `useState`, `CLOUD.set` ditambahkan di ~33 titik CRUD (lokasi 11, gudang 8, sub_gudang 5, satpam 2, uit 2, ultg 2, upt 2, tim_mutu 1) + 8 setter awal di `loadCloud()`, dibersihkan saat logout bareng Fase 1. Dikerjakan tukang-opus, direview arsitek penuh (diff dibaca, `npm run build` independen), diverifikasi user di localhost (edit Master Gudang/Lokasi/dll, refresh, data tetap ada). Catatan: 8 tabel ini TIDAK punya percabangan null/empty/has-data seperti 9 tabel Fase 1 — SENGAJA tidak diubah (di luar scope), tapi sudah aman by design (`seedMasterTableIfEmpty` benar dari awal, lihat insiden 2026-07-21 di atas).
- **Fix konsistensi null/empty/has-data untuk 8 tabel Fase 2 — DI-COMMIT & DI-PUSH ke `main` (`98a17c3`, 2026-07-23, deploy Vercel otomatis).** User minta digarap sekalian: efek nyata dari gap di atas adalah kalau fetch salah satu dari 8 tabel gagal di background, tampilan bisa ketimpa default/kosong TANPA toast peringatan (beda dari 9 tabel Fase 1 yang sudah benar). Fix: `seedMasterTableIfEmpty()`/`loadMasterTable(...).then(r=>r||[])` di `loadCloud()` Promise.all diganti `loadMasterTable(tableName)` mentah (bisa `null`), lalu percabangan 3-arah eksplisit ditambahkan persis meniru pola katalog/stocks yang sudah ada (baris ~588-611): null → HANYA `loadFailures.push(...)`, TIDAK PERNAH menulis ke server; ada data → pakai + refresh cache; genuinely kosong (untuk 6 tabel berDEFAULT_*) → seed sekali (perilaku sama seperti `seedMasterTableIfEmpty` lama); `ultg`/`sub_gudang` (tanpa DEFAULT_*) cukup 2-arah. `seedMasterTableIfEmpty` dihapus dari import App.jsx (tidak dipakai lagi di sana, definisi di `masterSync.js` DIBIARKAN, tidak dihapus). Ditangani dengan kehati-hatian ekstra karena menyentuh AREA YANG SAMA dengan "INSIDEN DATA PRODUCTION" 2026-07-21 (lihat "Keputusan arsitektur") — dikerjakan tukang-opus dengan instruksi eksplisit soal invariant "cabang null tidak pernah menulis ke server", direview arsitek baris-per-baris (bukan cuma baca laporan), `npm run build` independen. **Sempat ada insiden kekhawatiran saat testing user di localhost** ("banyak yang hilang", tanpa toast) — diselidiki TUNTAS: dicek langsung row count 8 tabel di database production (uit=1, upt=6, ultg=2, gudang=3, sub_gudang=8, lokasi=105, satpam=2, tim_mutu=2), lalu di-cross-check terhadap dump backup jam 02:00 (SEBELUM testing dimulai) via restore ke container sekali-pakai — angkanya IDENTIK, membuktikan TIDAK ADA data yang berubah/hilang di server sama sekali. Kesimpulan: kejadian itu murni kedipan tampilan sesaat (kemungkinan race kecil saat sesi browser baru dipulihkan), bukan bug data — user sendiri konfirmasi "sudah aman" setelah refresh ulang.
- **Root cause "Gagal memuat...Data Stok" + "foto tidak tampil" — DITEMUKAN & DIPERBAIKI (2026-07-23, operasi data langsung ke production, BUKAN perubahan kode App.jsx).** Muncul saat user testing Fase 2. Investigasi log Kong + SQL langsung: 7 dari 212 baris `stocks` MASIH punya foto base64 mentah ter-embed di `data.fotoKeseluruhan` (sisa dari migrasi SAP lama, termasuk baris `STK-MIG-1002090509` yang sempat dipulihkan dari insiden test-write sebelumnya) — total payload tabel `stocks` jadi ~24MB, membuat `GET /rest/v1/stocks?select=*` butuh ~16 detik dan KADANG timeout di client. Saat timeout, `loadCloud()` gagal fetch stocks → fallback ke cache `leanStocks()` (Fase 1, foto SENGAJA di-strip untuk hemat kuota) → user lihat foto kosong padahal sinkron "selesai". **Fix**: script bash one-off (dijalankan arsitek langsung via SSH ke `minipc-gudang`, BUKAN via Agent — task delegasi ke tukang-opus untuk ini SEMPAT diblokir classifier auto-mode karena menyangkut service_role key + migrasi data production) — upload ke-7 foto ke Storage bucket `stock-photos` (path `upt-surabaya/<no-katalog>/utama.jpg`, pola sama seperti 205 baris lain yang sudah benar), lalu `UPDATE stocks SET data = jsonb_set(data,'{fotoKeseluruhan}', url)` per baris. **Hasil**: payload tabel `stocks` turun dari ~24MB → **119KB**. Diverifikasi: query ulang `WHERE fotoKeseluruhan LIKE 'data:%'` = 0 baris, salah satu URL baru dicek HTTP 200 ukuran cocok persis, dan user konfirmasi langsung di app — toast "gagal memuat" hilang, semua foto muncul. **fotoNameplate tidak tersentuh** (sudah 0 baris embedded dari awal, semua sudah URL). Tidak ada perubahan kode/git commit untuk fix ini — murni migrasi data.
- **4 bug fix logout/cache/Auto-OCR — DI-COMMIT & DI-PUSH ke `main` (`0d91681`, `5447282`, `3d22d66`, `c2f9ffc`, 2026-07-23 malam, deploy Vercel otomatis) + migrasi 552 URL foto ke self-host (operasi data langsung, bukan commit kode).** User lapor "login di Vercel, password salah semua" — investigasi log Kong/GoTrue TIDAK menemukan satu pun percobaan login gagal (justru ada login sukses tercatat), jadi bukan soal password. Ternyata 2 rangkaian bug terpisah, ditemukan berurutan (masing-masing fix membuka bug berikutnya yang sebelumnya "ketutup"):
  1. **Logout tidak bersih**: `handleLogout()` (App.jsx) tidak pernah menghapus `PROFILE_CACHE_KEY`/`PHASE1_CACHE_KEYS`/`PHASE2_CACHE_KEYS` secara langsung — sepenuhnya bergantung listener async `onAuthStateChange` yang jalan SETELAH `signOut()` selesai. Kalau `signOut()` lambat ke server self-host dan user refresh duluan, sesi belum benar-benar putus (refresh = login lagi). Sekalian ketemu bug konsistensi terpisah: cabang "sesi kosong" di listener tidak pernah menghapus `PHASE2_CACHE_KEYS` (kebocoran cache 8 tabel master data antar-user di device sama). Fix (`0d91681`): `handleLogout()` sekarang membersihkan ketiga cache SECARA LANGSUNG (try/finally) + tombol Logout dapat state busy/disabled ("Keluar...") supaya user tidak refresh di tengah proses.
  2. **Loop "Memuat data dari cloud..." tak berujung**: begitu fix #1 live, cache stocks/katalog/lokasi jadi benar-benar kosong setelah logout — memicu bug LATEN yang sebelumnya nyaris tak pernah ke-trigger: `loadCloud()` (App.jsx) punya jalur "legacy migration" (cabang `else` saat cache lokal ketiga tabel itu kosong) yang HANYA `setState` ke React, TIDAK PERNAH `CLOUD.set()` ke localStorage. Akibatnya begitu cache kosong, jalur ini selalu terpakai dan tidak pernah menulis cache lagi → refresh berikutnya kosong lagi → loop selamanya. Fix (`5447282`): kedua sub-cabang jalur `else` sekarang menulis cache dengan nilai SAMA PERSIS yang di-set ke state (stocks tetap lean, tanpa foto base64); jalur normal (cache sudah ada) tidak disentuh.
  3. **Auto-OCR nameplate — canvas tainted**: terpisah dari 2 bug di atas (ditemukan dari screenshot console user). `compressImage()` (`src/lib/supabaseSync.js`) tidak set `crossOrigin="anonymous"` saat memuat foto dari URL remote (Storage) sebelum digambar ke `<canvas>` — browser anggap canvas "tainted", `toDataURL()` diblokir. Server (Storage self-host & cloud lama) sudah kirim `Access-Control-Allow-Origin: *` dengan benar (dicek langsung via curl) — yang kurang cuma sisi client. Fix (`3d22d66`): `crossOrigin="anonymous"` cuma untuk `srcUrl` http(s), bukan `blob:`/`data:` (upload File lokal tidak terpengaruh).
  4. **Auto-OCR nameplate — key kena error non-ASCII**: fix #3 bikin OCR sempat sampai tahap `fetch()` ke OCR.space, lalu gagal baru: `Failed to read the 'headers' property... String contains non ISO-8859-1 code point`. Pola PERSIS bug key Groq yang pernah terjadi (env var kesisipan whitespace non-standar dari Vercel). Fix (`c2f9ffc`): `VITE_OCRSPACE_API_KEY` di-`.trim()` di `src/lib/rag.js`, meniru pola `groqKey` di App.jsx.
  **Temuan terpisah saat investigasi (bukan bug kode, migrasi data)**: SEMUA 158 baris `stocks.fotoNameplate` + 197 baris `stocks.fotoKeseluruhan` + 197 baris `katalog.fotoKeseluruhanUrl` MASIH menunjuk ke project Supabase Cloud LAMA (`tadxodrzoquugnsyejld.supabase.co`) meski app sudah full self-host sejak 22 Juli — risiko nyata: semua foto ini akan 404 kalau project cloud lama itu suatu saat benar-benar dimatikan (saat ini sengaja dibiarkan hidup sebagai jaring pengaman rollback). Dicek via `scripts/migrate_storage_buckets.mjs --dry-run` (skrip migrasi FILE FISIK Storage yang ternyata SUDAH DIBUAT saat rencana migrasi self-host awal tapi belum pernah dijalankan) — hasil: 371 file/71.3MB, SEMUA sudah ada di self-host dengan path sama persis, jadi TIDAK perlu upload ulang. Fix: 3x `UPDATE ... SET data = jsonb_set(...)` ganti prefix domain URL ke `warnoto.com`, dijalankan arsitek langsung via SSH (delegasi Agent untuk operasi service_role key + data production, sama seperti insiden 2026-07-23 pagi, diblokir classifier auto-mode). **Hasil**: 0 baris tersisa merujuk cloud lama di ketiga field, sample URL baru dicek HTTP 200. Keempat fix kode diverifikasi independen oleh arsitek (diff dibaca + `npm run build` dijalankan ulang, bukan cuma percaya laporan tukang) sebelum push. **Catatan proses**: koneksi ke GitHub sempat sangat tidak stabil sepanjang sesi (connection reset & 403 bergantian, bukan soal kredensial) — semua push akhirnya berhasil lewat retry otomatis. User sudah tes ulang di `warnoto.vercel.app`: logout→refresh tidak lagi auto-login, "Memuat data dari cloud" tidak lagi loop, console bersih dari error OCR — dikonfirmasi aman.
- **Retry `loadMasterTable` + bersih Groq key — DI-COMMIT & DI-PUSH ke `main` (`0e65ac3`, 2026-07-23 malam lanjutan, deploy Vercel otomatis).** Menuntaskan 2 catatan minor lama: (1) `loadMasterTable()` (`src/lib/masterSync.js`) sekarang retry 1x (delay 1200ms) sebelum menyerah & `return null` — mengurangi frekuensi fallback ke cache lama saat 1-2 dari 17 query background sesekali gagal/timeout ke server self-host; contract null/array yang jadi pegangan `loadCloud()` TIDAK berubah. (2) `VITE_GROQ_API_KEY` di Vercel Production+Preview dihapus & di-set ulang bersih (56 char, dipastikan tanpa whitespace) via Vercel CLI. Dikerjakan tukang-sonnet, direview arsitek + `npm run build` ulang independen sebelum push.
- **Percobaan pindah `warnoto.vercel.app` → domain kustom `warnoto.co.id` — DITUNDA, dibiarkan menggantung (2026-07-23 malam lanjutan).** User minta setup domain baru via Vercel. Domain sempat di-attach ke project lewat `vercel domains add` — **ternyata ini otomatis menjadikannya domain utama & me-redirect `warnoto.vercel.app` lama ke domain baru, padahal DNS `warnoto.co.id` belum dikonfigurasi sama sekali → production sempat TIDAK BISA DIAKSES.** User memperbaiki manual lewat dashboard Vercel (mematikan redirect). `www.warnoto.co.id` sudah dilepas dari project. `warnoto.co.id` sendiri SENGAJA DIBIARKAN tetap terdaftar di Vercel team (bukan dihapus, supaya tak perlu daftar ulang) tapi TIDAK ADA DNS record yang mengarah kemanapun — registrar/pengelola DNS domain ini belum dipastikan user (disebut Cloudflare, tapi saat dicek `warnoto.co.id` belum delegasi nameserver ke manapun). **PENTING untuk vendor berikutnya kalau melanjutkan**: jangan attach domain baru ke project Vercel yang sedang live sebelum DNS-nya siap; kalau terpaksa attach dulu, langsung cek Vercel dashboard > Project > Domains untuk pastikan opsi redirect ke domain belum-aktif itu tidak mematikan akses `.vercel.app`.
- **Investigasi "Auto-sync Supabase gagal: Failed to fetch" + CORS massal di console — SELESAI, BUKAN bug (2026-07-23 malam lanjutan).** Muncul persis setelah deploy fix retry di atas. Diinvestigasi via curl langsung ke `warnoto.com` (paksa IP Cloudflare yang benar, `104.21.12.85`): preflight OPTIONS mengembalikan `access-control-allow-origin: *` dengan benar, 3x percobaan GET berturut-turut sukses — CORS & konektivitas server terbukti normal saat dicek. Sempat ada 1x kegagalan resolusi DNS transient di sisi arsitek yang langsung pulih sendiri di percobaan berikutnya. Kesimpulan: transient DNS/koneksi blip sesaat ke server self-host (persis kelas masalah yang baru saja ditangani fix retry), BUKAN regresi kode maupun masalah konfigurasi Kong/CORS. User konfirmasi error sudah tidak muncul lagi setelah refresh ulang.
- **Infrastruktur minipc-gudang — sesi 2026-07-21 (lanjutan, monitoring + backup gap + disk):** (1) `~/vps-observability` (Prometheus/Grafana/Alertmanager, dibangun sesi sebelumnya, sebelumnya belum tercatat detail di HANDOFF ini) diverifikasi ulang — 5 container sehat, Prometheus targets semua up, Grafana LAN-only (`http://10.91.21.231:3000`) terkonfirmasi HTTP 302, test alert Telegram end-to-end BERHASIL (`alertmanager_notifications_total{telegram}=1`, 0 gagal). (2) Gap backup auth/storage/vault ditemukan & ditutup, lihat poin 1 Keputusan Arsitektur (termasuk catatan insiden delegasi tukang-sonnet yang melanggar batasan file). (3) Disk root di-extend 100GB→936GB (879GB bebas) — lihat poin di bawah daftar 6 stack. (4) DR stack dinyalakan sebentar untuk user lihat data (Studio), lalu ditemukan mati lagi tanpa jejak jelas siapa yang mematikan — lihat poin 2 Keputusan Arsitektur.
- **Migrasi arsip legacy + UI history TUG-15 — SELESAI (Codex, 2026-07-28).** Pipeline production memuat 801 transaksi legacy, 401 dokumen, dan 732 lampiran/foto. Laporan Mutasi Stok sekarang menggabungkan sumber Baru/Lama tanpa mengubah saldo aktif, memuat arsip bertahap >1000 baris, menyediakan Quick Finder material, pencarian lintas pekerjaan/lokasi/vendor-ULTG/tanggal/dokumen/SPK-kontrak/catatan, drawer timeline dengan provenance yang tidak mengklaim lot-tracking, serta pagination 20/50/100. Build, graphify, 8 skenario Playwright desktop/mobile, snapshot responsif 390px, dan audit visual localhost lulus.
- **Langkah berikutnya (urut, bisa langsung dieksekusi):**
  0a. ~~Pantau stabilitas `warnoto-api-cloudflared` pasca-restart~~ — **SELESAI, protokol tunnel diganti ke `quic` (2026-07-27)**, lihat "Riwayat shift" untuk detail. **Masih perlu dipantau beberapa hari** (baru diuji jam-jam pertama) sebelum benar-benar dianggap tuntas jangka panjang — kalau `stream closed`/`i/o timeout` muncul lagi di `docker logs warnoto-api-cloudflared`, rollback via `docker-compose.yml.bak-20260727-115312` di `/home/admin_warnoto/vps-warnoto-api-tunnel/`. **Monitoring otomatis sudah dipasang** (2026-07-27): cron di `minipc-gudang` jam 10:00 UTC (17:00 WIB) tiap hari menjalankan `vps-warnoto-api-tunnel/scripts/check-tunnel-stability.sh` — hitung error `stream closed`/`i/o timeout`/`connection with edge closed` 24 jam terakhir + protokol aktif, kirim ringkasan ke Telegram (pakai bot yang sama dengan Alertmanager, `chat_id` sama) — **test kirim pertama DIKONFIRMASI user sudah masuk ke Telegram**. Rencana lanjutan (belum dikerjakan): replika cloudflared untuk HA, load-test sebelum transaksi live — user sudah setuju jalan bertahap dari sini.
  0b. ~~User verifikasi transaksi nyata TUG-8/TUG-9 di production~~ — **SELESAI (2026-07-30)**: TL Widi approve → Asman Rouuf approve final, `stock_movements` per-item cocok (before 6→after 3), riwayat approval & Laporan tampil benar. Detail lengkap di "Status sekarang".
  0d. **Whitelist SSL Inspection Fortinet untuk `warnoto.com` TIDAK PERSISTEN — perlu tindak lanjut ke IT.** IT sempat konfirmasi sudah whitelist (2026-07-30 pagi) dan sempat lancar, tapi error `ERR_CERT_AUTHORITY_INVALID` di `auth/v1/token` MUNCUL LAGI di hari yang sama — baik di PC lain (gejala beda: request menggantung "Memeriksa..." tanpa error, dikonfirmasi via DevTools Network `pending` terus) maupun di PC utama user (gejala sama seperti awal: error cert langsung). Dugaan: whitelist di-scope ke device/sesi tertentu bukan permanen di level Fortinet, atau ada software SSL-inspection terpisah (antivirus endpoint) di PC lain yang belum ikut ter-exclude. **Belum ada draft pesan follow-up ke IT** — perlu dibuatkan & dikirim ulang, tegaskan kali ini minta konfirmasi exclusion permanen (bukan sesi/device sementara) dan cek apakah ada software SSL inspection lain di luar Fortinet jaringan. **Tambahan temuan kode — SELESAI DIPERBAIKI (`6624b5f`, DI-PUSH ke `main`):** `handleLogin()` di `App.jsx` tidak punya timeout pada `supabase.auth.signInWithPassword()` — kalau network menggantung, tombol "Memeriksa..." bisa diam selamanya. Fix: bungkus dengan `_withTimeout()` (util yang sudah ada di `supabaseSync.js`, dipakai ulang) timeout 15 detik; pesan "Timeout login" otomatis lolos pola `isRetryableLoginError`/`describeLoginError` yang sudah mendeteksi kata "timeout" (retry 1x lalu tampil pesan jaringan yang sama seperti error cert). Belum diverifikasi visual browser (build lulus saja).
  0e. **Pending — uji gateway same-origin untuk jaringan kantor (Codex, 2026-08-01).** Karena WARNOTO lancar di Wi-Fi rumah tetapi `warnoto.com` sering terganggu Fortinet/Kaspersky di kantor, kandidat solusi adalah Cloudflare Worker pada path QA `pln.warnoto.com/__qa_supabase/health` yang meneruskan health-check ke Supabase self-host. Tahap awal hanya probe tanpa login/token dan tidak mengubah domain aplikasi, endpoint production, database, atau skema. **Belum diimplementasikan karena user sedang tidak berada di kantor**; lanjut hanya saat bisa diuji dari PC/jaringan kantor. Jika `warnoto.com` gagal tetapi endpoint QA berhasil, baru usulkan rollout bertahap Auth/REST/Storage/Realtime; jangan alihkan production sebelum pengujian kantor berhasil dan rollback endpoint lama tetap tersedia.
  0b2. **[MINOR, tidak blocking]** User lapor console browser (localhost) sempat menampilkan error `WebSocket connection to 'wss://warnoto.com/realtime/v1/websocket...' failed` (2026-07-30). Sudah dicek arsitek: container `realtime-dev.supabase-realtime` healthy, tunnel `warnoto-api-cloudflared` tidak ada error mendekati waktu kejadian, `curl` handshake pakai apikey yang sama berhasil `101 Switching Protocols`, dan reload browser menunjukkan app normal (dashboard tampil lengkap, tidak ada banner "Koneksi realtime terputus", console bersih). Kemungkinan besar cuma noise retry awal koneksi Supabase realtime client, bukan regresi kode. Kalau muncul lagi DAN disertai banner nempel/data tidak sinkron di UI (bukan cuma console), baru investigasi lebih dalam.
  0c. **User verifikasi visual TUG → Laporan setelah deploy:** klik area baris harus membuka detail satu transaksi; klik tombol Riwayat lengkap pada baris yang sama harus membuka seluruh history material, dengan filter arah dan penanda warna yang benar.
  0. ~~PRIORITAS: User verifikasi visual browser end-to-end pasca-refactor App.jsx~~ — **SELESAI**, user sudah cek menu Maturity di localhost & minta beberapa perbaikan visual (lihat "Status sekarang"), sudah di-commit+push (`94cd3d4`). Sisa tab lain (Alat Berat/Approval/Master Data/Data Stok/sidebar/header/modal lain) belum eksplisit dilaporkan dicek user — kalau ada laporan aneh di tab tsb pasca-refactor, cek dulu apakah regresi refactor sebelum root-cause ke tempat lain.
  1. ~~User commit manual `.claude/settings.json`~~ — **SELESAI**, sudah ikut ter-commit bareng fix sync (`5dff883`), tidak perlu aksi manual lagi.
  1b. ~~Loading awal lambat / auto-logout / pesan password salah di localhost~~ — **SELESAI**: cache-first lama (`a3f2b89`/`8101c3a`) dilengkapi retry (`0e65ac3`) dan patch branch `feature/import-attb-maturity` yang mengikat env/token/cache ke `warnoto.com`, mempertahankan sesi saat fetch profil transien, mengklasifikasi error login, serta melepas layar blocking setelah tiga dataset kritis siap.
  1c. ~~Lanjutkan setup domain custom untuk ganti `warnoto.vercel.app`~~ — **SELESAI DENGAN PENDEKATAN BEDA (2026-07-31)**: bukan `warnoto.co.id` (batal, tidak jadi beli), tapi subdomain `pln.warnoto.com` (sudah dimiliki user di Cloudflare). Setup: CNAME `pln`→`cname.vercel-dns.com`, attach ke Vercel, verifikasi dulu baru redirect 308 `warnoto.vercel.app`→`pln.warnoto.com` diaktifkan (hindari insiden redirect 2026-07-23). Proxy Cloudflare (awan oranye) juga diaktifkan untuk `pln` guna atasi `ERR_TIMED_OUT` intermiten di jaringan seluler. **User konfirmasi login lancar (2026-07-31).**
  1d. ~~Vendor A lanjut dari branch `feature/import-attb-maturity`~~ — **SELESAI**, sudah di-merge ke `main` (`db4d429`) bareng fix scoping UPT, live di `warnoto.vercel.app`.
  1e. ~~User verifikasi visual browser: redesain banner Maturity + 6 fitur business-logic baru~~ — **SELESAI**, sudah di-cek user + di-push bareng refactor App.jsx (`94cd3d4`).
  1f. **Konfirmasi ulang widi (TL, UPT Surabaya):** picker Maturity sempat tampil "Gresik" bukan "Surabaya" di localhost — data DB sudah dicek benar (`upt_id=UPT-SBY`), dicurigai state HMR/browser nyangkut. Minta widi hard-refresh (Ctrl+Shift+R) dan konfirmasi ulang.
  2. **User verifikasi hasil deploy UI mobile ATTB** (`3d572a3`) serta perubahan lokasi Data Stok oleh ADMIN (`15668b1`). Perubahan ATTB lanjutan tetap melalui `/uimobile` dan ownership gate ATTB.
  2a. **Realtime Data Stok sudah selesai dan aktif** (`09001e9`); pantau penggunaan normal. Jika sinkron antarperangkat bermasalah, cek health `realtime-dev.supabase-realtime`, log Realtime, membership `supabase_realtime|public.stocks`, dan status kedua replication slot sebelum mengubah frontend.
  2b. ~~Vendor berikutnya mengimplementasikan revisi Inspeksi Material Cadang yang sudah disetujui.~~ — **SELESAI, DI-COMMIT & DI-PUSH** (`8f611d8`, 2026-07-27). DDL production sudah jalan, rollout atomik selesai (lihat "Status sekarang"). User belum verifikasi visual browser end-to-end fitur baru (Buat Inspeksi multi-material + History BA) — perlu dicek sebelum dianggap tuntas total.
  3. User verifikasi visual fix `a067196` khusus bagian **Forecast Stok & Stock Opname** (bagian ATTB-nya sudah masuk scope poin 2 di atas) di viewport mobile.
  4. User cek hasil install PWA di HP setelah deploy `96bffab` — konfirmasi logo ikon home-screen sudah benar (Android & iOS kalau ada keduanya).
  5. ~~User cek dokumen BA TUG-10 hasil cetak/unduh: layout 4 kolom tanda tangan~~ — **USANG, digantikan template baru (2026-07-31):** TUG-10 (dan TUG-3/4, TUG-5) sudah diganti total ke gaya AppSheet 3-halaman/4-halaman senada TUG-9 (`679d8ef`). Item baru: **user cek transaksi TUG-3/5/10 nyata pasca-template baru** — verifikasi sebelumnya cuma pakai data sintetis di dev server (0 transaksi TUG-3/5/10 asli di production saat itu), belum ada pengecekan preview/PDF dengan data transaksi sungguhan.
  6. Migrasi Non-SAP UPT Surabaya review-first: 40 baris hasil audit (34 kuat, 5 lemah, 1 tanpa kandidat), via UI Opname Non-SAP → Upload Usulan Pencocokan — BUKAN write langsung ke Supabase. **Tetap ditunda (dikonfirmasi ulang 2026-07-22) — jangan mulai sampai ada arahan lanjut.**
  7. i18n masih ditunda, belum ada keputusan mulai kapan — tunggu arahan user.
  8. **User verifikasi end-to-end fix Kapasitas Gudang**: buka `npm run dev` (data lokal masih ada di browser) supaya bootstrap-push otomatis jalan ke Supabase dengan skema yang sudah benar, lalu cek data juga muncul di Vercel (bukan cuma localhost). Lihat "Keputusan arsitektur" untuk detail fix.
  8a. **User verifikasi production Peta Utilisasi setelah deploy 2026-08-02:** bandingkan satu titik Ketintang dan satu titik Gudang Semi Terbuka 2 Wonorejo dengan posisi yang sama di Master Data → Master Gudang → Kelola Denah & Koordinat Sub Gudang.
  8b. **Lanjutkan penambahan History Audit Maturity:** data awal UPT Surabaya sudah canonical; tahap berikutnya adalah menyepakati alur input/edit history tambahan dan pembatasan role sebelum membuat UI tulis.
  8c. **User verifikasi production Form 5S setelah deploy:** simpan satu pengisian nyata, refresh halaman, lalu pastikan record muncul di Form Pengisian 5S → History Audit 5S dengan skor, gudang, auditor, catatan, dan detail indikator yang sama.
  8d. **User verifikasi production evidence Pelaksanaan Audit Maturity pada audit nyata pertama:** upload satu evidence, refresh/sinkronkan Drive, unduh ulang, dan pastikan folder terbentuk sesuai bulan → UPT → kategori → aspek → item. Jangan membuat audit dummy karena hanya satu audit per UPT/periode.
  9. **User verifikasi hasil deploy UI mobile** pada Dashboard, Alat Berat, dan Data Stok. Setelah diterima, lanjutkan parity Dashboard ke `DashboardAsman.jsx` dan `DashboardManager.jsx` (termasuk disclosure kartu UPT Manager) serta snapshot actor-specific.
  10. **User verifikasi visual browser: upload foto baru pasca-fix `ff7fd49`** (Data Stok tombol Simpan Foto, modal Tambah/Edit Data Stok, material baru Stock Opname Non-SAP, edit foto Alat Berat) — pastikan foto benar-benar tersimpan sebagai URL Storage (bukan base64) dan tidak ada error upload di kondisi jaringan normal.
  11. **Klarifikasi status `vps-dr-stack`** (lihat "Keputusan arsitektur" poin 2, koreksi 2026-07-28): apakah production dianggap tetap di sini permanen, atau ada rencana pindah ke stack terpisah sesuai desain DR semula.
  12. ~~Push pipeline arsip legacy history AppSheet (`e62f4cc`)~~ — **SELESAI**, user memberi izin commit dan push pada 2026-07-28; pipeline ikut diterbitkan ke `main` bersama hasil sesi Codex.
  13. ~~Eksekusi data nyata pipeline arsip legacy history UPT Surabaya~~ — **SELESAI**, production berisi 801 transaksi legacy, 401 dokumen, dan 732 lampiran/foto; data arsip tetap terpisah dan tidak mengubah saldo stok aktif.
  14. ~~Implementasi React UI Laporan Mutasi Stok untuk history gabungan dan pencarian material~~ — **SELESAI**, termasuk Quick Finder, pencarian lintas atribut, drawer timeline, filter sumber, export gabungan, dan pagination 20/50/100; sudah diverifikasi desktop/mobile di localhost.
  15. **Ide fitur analitik (belum diprioritaskan, 2026-07-28):** dua insight di atas data yang sudah ada (bukan skema baru) — (a) dashboard "frekuensi pengambilan per lokasi/rak" dari `stocks.lokasi_id`+`tug15_history.lokasi_kode` → rekomendasi re-slotting rak gudang; (b) auto-flag selisih `stock_opname`/`stock_count` yang di luar toleransi (cek dulu apakah sekarang masih murni pencatatan manual atau sudah ada alert) → early-warning sebelum jadi temuan audit BPK/internal. Perlu arahan user kapan mulai digarap.
  16. **User verifikasi deploy menu Alat Berat sebagai Admin Gudang:** coba tambah satu alat nyata dan edit seluruh field/foto; pastikan toast sukses muncul dan data tetap ada setelah refresh/perangkat lain. Jika gagal, jangan tutup modal—cek sesi login dan koneksi `warnoto.com`.
- **Selesai/clear (dikonfirmasi user 2026-07-22):** sisa 51 baris Data Stok tanpa lokasi sudah diisi manual; Fajar (SUPERADMIN) sudah logout/login (cache profil refresh); `gudangId` + foto satpam sudah diisi. Tidak perlu tindak lanjut lagi untuk ketiganya.
- **Blocker:** **Production browser MEMERLUKAN whitelist IT `warnoto.com` di firewall Fortinet/Kaspersky kantor PLN.** Gateway same-origin tetap pending sampai dapat diuji dari jaringan kantor. Integrasi Google Drive sudah aktif dan bukan lagi blocker; upload foto khusus Form 5S masih merupakan pekerjaan terpisah.

## Perintah verifikasi
- `npm run dev` → port 3001 (akses via `localhost`)
- `npm run build`
- `node --test tests/unit/tugCanonical.contract.test.mjs tests/unit/telegramWebhook.contract.test.mjs`
- `node scripts/rehearse_tug_canonical_postgres.mjs`
- `npx playwright test tests/e2e/desktop.spec.js --grep "canonical derived TUG-8 draft"`
- `node --test tests/unit/heavyEquipmentPhoto.test.mjs tests/unit/stockLocationApproval.test.mjs`
- `npx playwright test tests/e2e/heavy-equipment.spec.js --project=desktop-smoke --workers=1`
- `npx playwright test tests/e2e/desktop.spec.js --project=desktop-smoke --grep "ATTB preview|cache-first stock detail"`
- `npx playwright test tests/e2e/tug15-legacy.spec.js --project=desktop-smoke --project=tug15-mobile --workers=1`
- `npx playwright test tests/e2e/responsive.spec.js --project=phone-390 --grep "tug-15 obeys"`
- `npx playwright test tests/e2e/desktop.spec.js --project=desktop-smoke --grep "maturity file upload"`
- `node --test tests/unit/maturityDrive.security.contract.test.mjs`
- `npx playwright test tests/e2e/desktop.spec.js --project=desktop-smoke --grep "Form 5S"`
- Verifikasi DB history: query count `maturity_audit_history` dan urutkan `(tahun,semester)` via `minipc-gudang-home`; expected 5 baris UPT Surabaya untuk seed awal.
- Deploy setelah persetujuan eksplisit user: `git push origin main`

## Riwayat shift (maksimal 2)
- 2026-08-02 Codex: Menyelesaikan skill/UI Maturity hari ini hingga evidence Google Drive + metadata/history self-host (`7cc7578`), termasuk migration production, OAuth, Edge Function, dan hardening scope; di-push ke `main`.
- 2026-08-01 Claude: **Perluas minQty computed-first ke Dashboard & bot Telegram, di-push ke `main` (`ef40771`).** Ternyata ada 3 implementasi terpisah konsep "kritis" (getKritisAgg/getMaterialAkanHabis/bot) — dipusatkan jadi satu formula di `ragShared.mjs`. Detail lengkap di "Status sekarang".
